# Changelog

## [Unreleased]

### Hinzugefügt
- **Production-Scoring als Zwei-Flow-System ausgebaut.** Neben dem Datei-Flow (`production/run_scoring.py`, Datei → XPT) neuer saspy-Flow `production/run_scoring_saspy.py` (SAS-Library → SAS-Library: gechunkter `sd2df`-Pull mit `keep=`/`where=`, Rückschreiben via `df2sd` + `PROC APPEND` in `replace`/`append`, log-geprüfte submits, `saspy.setup_code`, `endsas` im finally). Gemeinsamer Scoring-Kern `score_dataframe` für beide Flows. saspy als optionales Extra `.[saspy]` + pixi-prod; Task `scoring-saspy`.
- **Ein-Lade-Modus für mehrere Bundles gegen dieselbe Quelle.** Eine Scoring-YAML kann statt `bundle`/`scoring`/`output` eine `scores:`-Liste tragen (je Eintrag `name`/`bundle`/optional `scoring`/`output`): Bundles werden zuerst geladen, die Quelle genau EINMAL gelesen (Spalten = Union aller Bundle-Features + IDs) und pro Eintrag mit dessen Preprocessor/Modellwahl verarbeitet. Fehler-Semantik: nicht ladbare Bundles brechen atomar ab (vor jedem Lesen/Schreiben); Laufzeit-Fehler späterer Einträge lassen frühere Ziele bestehen. Monitoring je Eintrag (`<name>_<eintrag>_….json`, `duration_seconds` pro Eintrag + `total_duration_seconds`). In beiden Flows; per Zähler-Tests und E2E (disjunkte Bundles, eine breite Tabelle) verifiziert.
- **Generisches Job-Skript `production/run_scoring.sh`** (ersetzt das PH-gebundene Skript): `SCORING_CONFIGS`-Liste fährt beliebig viele Use-Cases in je eigenem Python-Prozess; Top-Level-Key `runner: file|saspy` pro YAML routet den Einstieg, gemischte Jobs möglich; `CONTINUE_ON_ERROR`, `RUNNER_SCRIPT`-Override; Override-Guards (Multi bzw. saspy + `INPUT/OUTPUT_OVERRIDE` → Exit 2 mit Meldung). Exit-Codes dokumentiert (0/2/rc/1).
- **Kopiervorlagen + Betriebsanleitung:** `scoring_template_file.yml` / `scoring_template_saspy.yml` (jedes Feld PFLICHT oder optional mit Default markiert) und `production/PRODUKTIVSETZUNG.md` (Flussdiagramm, Rollen-Tabelle, Pflicht/Kann-Tabellen mit Flow-Spalte, Erstlauf-Checkliste, Fehlermeldungs-Tabelle mit Original-Wortlauten). `scoring_ph.yml` bleibt als gelebter Use-Case.
- **Numerische Referenztests für die Uplift-Metriken** (`tests/test_uplift_metrics_reference.py`): Qini/AUUC exakt gegen sklift-Kurvenflächen (via `importorskip`, keine neue Pflicht-Dependency) und sklift-freie Hand-Trapez-Referenz; `uplift_at_k` exakt gegen sklift; `policy_value` gegen Differenz-in-Means; MT: Arm-Kurven bitidentisch zur BT-Maschinerie auf der {Kontrolle ∪ Arm}-Teilmenge (transitive sklift-Verifikation), `policy_value_mt` gegen Hand-IPW, `optimal_treatment_assignment` gegen argmax-Referenz, `mt_eval_summary`-Konsistenz. Modell-Ranking in allen Trials identisch zu sklift.

### Geändert
- **Dependency-Hygiene nach Produktions-Segfault (Exit -11 im BLT):** `flaml` vollständig entfernt (kein Import mehr im Code, UI und Configs referenzieren es nicht — reiner Deklarations-Ballast in pixi/pyproject). `pyreadstat` und `saspy` von conda- auf **PyPI-Dependencies** umgestellt (Haupt- und prod-Block): PyPI-Pakete nehmen am Conda-Solve nicht teil — als conda-Dependencies hatten sie mit Einführung des prod-Features über die gemeinsame solve-group den kompletten geteilten Stack neu ausgehandelt (u. a. llvm-openmp-/numpy-Builds) und damit die native Basis unter CatBoost verschoben; der Faulthandler-Stack des Segfaults zeigt 20 parallele native CatBoost-Trainings. Die solve-group-Garantie für die pickle-relevanten Pakete (econml/lightgbm/scikit-learn/numpy/pandas) bleibt unverändert bestehen.
- **Scoring-Config-Loader validieren strikt:** unbekannte Schlüssel werden hart abgelehnt (difflib-Vorschlag „meinten Sie …?"; `output.meta_columns` bleibt Free-Form), Kreuz-Runner-Configs nennen den richtigen Einstieg, `id_columns` ist Pflicht (ohne IDs wären Score-Zeilen im Zielsystem nicht zuordenbar). `SCORE_B`-Default in beiden Flows vereinheitlicht auf `null` (kein stiller Surrogate — was scort, steht explizit in der YAML). Neue Input-Optionen `csv_sep`/`csv_encoding`; parquet-Spalten-Pruning behält die Datei-Reihenfolge (Set-Iteration war nichtdeterministisch). Modell-Pickles laden lazy (`_LazyModelStore`); Feature-Guard: fehlen ALLE Bundle-Features im Input (klassischer `uppercase_columns`-Mismatch), bricht der Lauf hart ab statt NaN-Scores zu schreiben, einzelne fehlende Spalten warnen.
- **Test-Suite kuratiert (252 → 240, Laufzeit 34s → 14s):** Loader-Validierungs-Mikrotests zu zwei Sammeltests konsolidiert, Zwei-Richtungs-Verträge zusammengelegt (Feature-Guard, scores-Modus), saspy-Redundanzen über den gemeinsamen Kern gestrichen, Referenz-Trials gestrafft. `rubin_overview.html` und `docs/` auf den beschriebenen Stand gebracht (u. a. Ensemble-Serialisierung rubin-eigen statt EconML, External-Refit-Ausnahme, Abhängigkeiten pyreadstat/saspy, Modus-Kontext der externen K-Fold-CV). `.gitignore`: `.ruff_cache/` ergänzt.

### Behoben
- **Qini-/AUUC-Integral inkl. Ursprung (0, 0):** Die Kurvenpunkte beginnen bei k=1; das Trapez-Integral ließ das erste Segment ab dem Ursprung weg — ein O(1/n)-Diskretisierungsrest (~0,008 % relativ), der die dokumentierte sklift-Gleichheit knapp verfehlte. Mit Ursprung im Integral stimmen die Flächen exakt mit `sklift.qini_curve`/`uplift_curve` überein; ranking-invariant (200/200 Trials identische Modell-Reihenfolge), Golden-Snapshots unverändert. Verbleibende ~5e-5-Abweichungen in Einzelfällen sind die dokumentierte Jitter-Präzisionsgrenze (Nachbar-Scores mit Abstand < 1e-9·max|score| gelten als effektiv gleich; Docstring präzisiert).
- **MT im Production-Scoring erstmals End-to-End verifiziert:** `column_order`-Expansion `SCORE_P` → `SCORE_P1, SCORE_P2` an Position, `extra_models` → `CATE_<Name>1/2`, Monitoring-Statistiken je Arm-Spalte; V5×MT-Kürzungs-Kollision (`CATE_Ens`) wird hart abgelehnt (Fehlertabelle ergänzt: bei MT mit `extra_models` ist V8 praktisch Pflicht); MT-Ensemble == Mitgliedsmittel pro Arm exakt (0.0). Zudem live bestätigt: External-Bundle-Scoring, Optuna-BLT-Minilauf, `run_explain` (SHAP, plausible Importance), `run_promote` (Registry als operative Wahrheit bis in die ProductionPipeline), Flask-API-Smoke.

### Geändert (früher in dieser Arbeitsphase)
- **Terminologie durchgängig auf „Forest" vereinheitlicht** (fachlich korrekter Begriff, auch im Deutschen). Das bisher stellenweise verwendete deutsche Synonym für den GRF-Baumbestand wurde repo-weit durch „Forest" ersetzt — in Code-Kommentaren/Docstrings (`model_registry.py`, `settings.py`, `tuning/{causal_forest,base_learner,final_model}.py`, `pipelines/analysis_pipeline.py`, `reporting/html_report.py`), UI-Texten (`app/src/{01_constants,11_page_tuning}.jsx` sowie die gebauten Kopien `app/rubin_ui_src.jsx` und `app/frontend/index.html`), Docs (`README.md`, `docs/{architektur,konfiguration,tuning_optuna}.md`, `configs/config_reference_all_options.yml`) und `rubin_overview.html`. Betroffene Report-Ausgabestrings (u. a. „kausale Forest-Parameter", „Gefundene Forest-Parameter") wurden synchron in den Golden-Snapshots (`tests/data/report_golden{,_external}.html`) nachgezogen. Rein textuell; kein Code-Identifier und keine base64-Bilddaten betroffen.

### Behoben
- **TMES-Modus („Train Many, Evaluate Some") End-to-End verifiziert — keine Code-Änderung nötig.** Kompletter Lauf mit `.npy`-Eval-Maske (500 von 2500 Zeilen): Maske korrekt erkannt und angewendet (Metriken/Plots auf n=500, Training/OOF auf allen Zeilen), Ensemble auch in diesem Modus präsent (der External-Index-Fix trägt hier ebenfalls), Full-Data-Refit auf allen 2500 Zeilen (konsistent mit der TMES-Semantik: Maskenzeilen sind kein Trainings-Holdout, Leakage-Freiheit via OOF), Bundle mit 4 Modellen production-scorebar, MLflow-Params `tmes_total_n`/`tmes_eval_n` geloggt. Beobachtung ohne Änderung: Eine Maske mit falscher Länge wird per Warning still verworfen und der Lauf degradiert zu reinem CV — dokumentiertes Verhalten, aber im Log leicht zu übersehen. Damit sind alle vier Validierungsmodi (CV, External, Multi-Treatment, TMES) End-to-End abgedeckt.
- **Multi-Treatment war mit NonParamDML nie lauffähig — jetzt konsistent als Binary-only deklariert (dritter E2E-Fund, vorbestehend).** econml beschränkt das nichtparametrische Final-Model von `NonParamDML` auf „single-dimensional continuous or binary categorical treatment"; mit `discrete_treatment=True` (rubin-Standard) also faktisch Binary-only. Ein MT-Lauf mit NonParamDML crashte erst **mitten im Training** (`AttributeError` in econml `_FinalWrapper.fit`) — Validator, UI und sogar die Referenz-Config `config_multi_treatment.yml` behandelten das Modell aber als MT-fähig. Fixes über alle Schichten: `NonParamDML` in `_BT_ONLY_MODELS` (`settings.py`, mit Begründung) → die Config-Validierung lehnt MT+NonParamDML jetzt sofort mit klarer Meldung ab; UI-`btOnly`-Set in `01_constants.jsx` + gebaute Kopien (Modell wird im MT-Modus ausgeblendet und aus den Defaults gefiltert); `config_multi_treatment.yml` auf ParamDML/DRLearner/CausalForestDML umgestellt; Doku (`konfiguration.md`, `rubin_overview.html`-Warnbox). Tests: `test_treatment_config_multi` und `test_mt_allows_dml` nutzten ausgerechnet NonParamDML als „MT-kompatibles" Beispiel — auf ParamDML umgestellt; neuer `test_mt_blocks_nonparamdml`.
- **`_predict_effect` crashte bei Multi-Treatment mit mittiger Outcome-Achse (vierter E2E-Fund, vorbestehend).** econml liefert Effekt-Shapes nach der `(n, d_y, d_t)`-Konvention, kollabiert Singleton-Achsen aber je nach Version/Modell unterschiedlich — bei LinearDML/MT unter econml 0.16 kommt `(n, 1, K−1)` mit der Singleton-**Outcome**-Achse in der Mitte. Das bisherige reine Trailing-`squeeze(axis=-1)` traf dort die Treatment-Achse → `ValueError: cannot select an axis to squeeze out`, MT-Training brach ab. Fix: dimensionsbewusste Normalisierung (erst Trailing-Singletons, dann explizit die mittlere `d_y=1`-Achse; echt unerwartete Shapes werden mit klarer Meldung abgelehnt statt still verbogen). Neuer Test `test_predict_effect_normalizes_all_econml_shapes` deckt alle sechs Varianten plus den Ablehnfall ab. **Multi-Treatment damit erstmals End-to-End verifiziert:** kompletter MT-Lauf (3 Arme, 3000 Zeilen, ParamDML+DRLearner+CausalForestDML, 57 s) → Bundle mit Ensemble + MT-Surrogate → Production-Scoring mit allen Arm-Spalten (`cate_*_T1/T2`, `optimal_treatment_*`, `treatment_confidence_*`), `ShapeSafeEnsembleCate`-MT-Output korrekt `(n, K−1)`, Ensemble-Arme bis auf Float-Epsilon (≤1,7e-16) exakt das Mitgliedsmittel. Suite: 199/199.
- **Ensemble fehlte im External-Eval-/Holdout-Modus still (Längen-Mismatch beim OOF-Frame-Aufbau) — zweiter E2E-Fund, vorbestehend.** Beim Ensemble-Aufbau in `run()` wurde der DataFrame-Rahmen mit `index=range(len(X))` (Train-Länge) erzeugt, die Prediction-Frames haben im External-/Holdout-Modus aber Eval-Länge → `ValueError: Length of values (n_eval) does not match length of index (n_train)`, vom umgebenden try/except geschluckt („Ensemble-Erstellung fehlgeschlagen"-Warnung), Ensemble fehlte kommentarlos in Metriken, Report und Bundle. Im CV-Modus fiel es nie auf (OOF-Länge == Train-Länge). Fix: Rahmen wird am ersten Prediction-Frame ausgerichtet (`pd.RangeIndex(len(first_df))`), modus-agnostisch. **End-to-End verifiziert (External-Eval-Lauf, 2500 Train / 800 Eval, gemischte Familien):** Ensemble wird erstellt und evaluiert (n=800), erscheint als 4. Modell in Metriken/Bundle; zusätzlich erstmals der **Holdout-Zweig des Refit-All-Exports dynamisch bestätigt** — keine Full-Data-Refits (Modelle bleiben Train-gefittet), `metadata.json` mit `champion_refit_on_full_data=false`/`refit_all_on_full_data=false`, Ensemble-Mitglieder tragen `_d_t_in` (Single-Fit), Production-Scoring des External-Bundles (Champion+Ensemble, n=800) fehlerfrei.
- **UI→Backend-Vertrag erstmals ausgeführt:** Die echte `buildYaml`-Logik aus `12_yaml_export.jsx` wurde via Node/Babel für vier Szenarien evaluiert (UI-Defaults; Seed 0/0; Vollausbau mit Ensemble/FS/Surrogate/Bundle/festen Hyperparametern inkl. `learning_rate: 0.002`; Multi-Treatment + External) und jede erzeugte YAML durch `rubin.settings.load_config` (Pydantic, `extra="forbid"`) validiert — alle vier valide, inkl. der zuvor gefixten Roundtrips (`SEED: 0` bleibt 0; `SEED: 21`; Dezimalwert 0.002 kommt exakt an). Reiner Verifikationslauf, keine Code-Änderung.
- **Ensemble-Scoring crashte bei gemischten Modellfamilien („inhomogeneous shape") — gefunden durch echten End-to-End-Lauf, behoben via `ShapeSafeEnsembleCate`.** Ein vollständiger Pipeline-Lauf (3000 synthetische Zeilen inkl. kategorialem Feature, TLearner + NonParamDML + CausalForestDML, Ensemble, Surrogate, Bundle-Export) deckte auf: EconMLs `EnsembleCateEstimator.const_marginal_effect` mittelt die Mitglieder-Outputs per nacktem `np.average` über eine Python-Liste — bei heterogenen Output-Shapes gemischter Familien (Meta-Learner liefern `(n,)`, DML/DR `(n,1)` bzw. `(n,1,1)`) crasht das mit `ValueError: … inhomogeneous shape`. Der Fehler war **latent auch im alten Code** (jeder direkte Aufruf des Ensemble-Objekts mit gemischten Familien, z. B. Ensemble-Champion beim Surrogate-Training oder Production-Scoring) und blieb unentdeckt, weil der Analyse-Lauf Ensemble-Metriken durch Mitteln der OOF-DataFrames berechnet und der bisherige Regressionstest zwei *gleichartige* Mitglieder nutzte. **Fix:** Neue Klasse `ShapeSafeEnsembleCate` (`rubin/training.py`) — normalisiert jedes Mitglied über `_predict_effect` (BT → `(n,)`, MT → `(n, K−1)`), mittelt erst danach (gewichtete Mittelung, Default gleichgewichtet), lehnt echte Inkompatibilitäten (unterschiedliche Treatment-Dimensionen) mit klarer Meldung ab; self-contained (Mitglieder werden mitserialisiert), API-kompatibel (`const_marginal_effect`/`effect`/`cate_models`/`weights`). Ersetzt `EnsembleCateEstimator` an beiden Konstruktionsstellen (Trainings-Ensemble in `_run_training` und Export-Rebuild in `_run_bundle_export`). Neuer Test `test_shape_safe_ensemble_handles_heterogeneous_member_shapes` ((n,)/(n,1)/(n,1,1)-Mitglieder → exaktes Mittel; EconML-Original als Crash-Referenz; MT-Dimensionskonflikt → ValueError). **End-to-End verifiziert:** kompletter Analyse-Lauf (41 s) → Bundle mit 5 Modellen → `run_production.py --use-all-models` (der zuvor crashende Aufruf) sowie `--use-surrogate` und Python-API (Champion+Ensemble in einem Aufruf) erfolgreich; `cate_Ensemble` stimmt bis auf Float-Epsilon (1,7e-16) mit dem manuellen Mitgliedsmittel überein. Suite: 197/197.
- **pandas-3-Kompatibilität (String-Migration: Dtype „str" statt „object").** Unter pandas ≥3 melden Textspalten den neuen `str`-Dtype; das brach den Schema-Vergleich (2 Test-Fails in `test_schema_utils`, weil persistierte Schemata `object` erwarten) und hätte zwei weitere Stellen still degradiert: die `select_dtypes(["category","object"])`-Listen (liefen nur noch über einen Deprecation-Shim, `Pandas4Warning`) und — am kritischsten — die `dtype == "object"`-Checks der DataPrep-Auto-Coercion (`data_prep_pipeline.py`), die `str`-Spalten unter pandas 3 unbemerkt übersprungen hätten (CSV liest dort per Default `str`). Fixes: zwei zentrale Helfer in `rubin/utils/data_utils.py` — `object_like_selectors()` (versionsabhängige Selektorliste; unter pandas 2 nur `object`, da `str` dort kein gültiger Selektor ist) und `is_object_like_dtype()` (Äquivalenzklasse object/str/string). Angewendet in `preprocessing.py` (2×: numerische Spaltenermittlung im Fit, kategoriale Auto-Erkennung), `utils/categorical_patch.py` und `pipelines/data_prep_pipeline.py` (2× Coercion-Checks). `schema_utils.validate_schema` normalisiert object/str/string vor dem Vergleich (Rohwerte bleiben im Mismatch-Report erhalten) — damit validieren auch bestehende pandas-2-Bundles unter pandas 3 und umgekehrt. Neuer Test `test_validate_object_str_string_equivalent` deckt beide Richtungen plus echte Mismatches ab. **Empirisch verifiziert: volle Suite grün unter pandas 2.3.3 (195/195) UND pandas 3.0.3 (196/196 nach neuem Test; vorher 193/2 Fails).** Der pixi-Pin `pandas >=2,<3` kann unverändert bleiben; ein späteres Upgrade auf pandas 3 ist an diesen Stellen nun vorbereitet.
- **Feste Hyperparameter: Dezimal-Komma und -Punkt sind jetzt gleichwertig („0,002" == „0.002"), unabhängig vom Browser-Locale.** Bisher nutzten die numerischen Felder des `FixedParamsEditor` `type="number"` + `Number(value)`: In einem deutsch eingestellten Browser normalisierte meist der Browser selbst, in einem en-Locale-Browser war „0,002" jedoch *badInput* → `value` wurde `""` → der Wert fiel **still auf den Default zurück** (der Nutzer glaubte 0,002 gesetzt zu haben, wirksam war z. B. 0.03). Neu: numerische Felder sind Text-Inputs mit `inputMode="decimal"` und explizitem, locale-unabhängigem Parser — ein einzelnes Komma wird als Dezimaltrenner behandelt (`0,002 → 0.002`), Zwischenzustände beim Tippen („0,") werden über einen Draft-State gehalten, ambige Eingaben (Komma **und** Punkt wie „1,000.5", mehrere Kommas, „abc") werden **nicht** committet (letzter gültiger Wert bleibt bestehen), leeres Feld setzt weiterhin auf den Default zurück, wissenschaftliche Notation (`1e-3`) bleibt möglich. Der Reset-Knopf (↺) leert nun auch den Draft. YAML-Export unverändert korrekt (JS-Zahlen serialisieren stets mit Punkt). Abgrenzung: Die übrigen `Inp`-Zahlenfelder (Trials, Folds, Timeouts, Sample Size, …) sind reine Ganzzahlfelder ohne Dezimal-Szenario und bleiben `type="number"`. Synchron in `app/src/02_components.jsx`, `app/rubin_ui_src.jsx`, `app/frontend/index.html`; Parser-Semantik über 19 Eingabeformen empirisch verifiziert.
- **UI-Audit aller Slider/Numerik-Eingaben inkl. YAML-Roundtrip — 5 Fehlerklassen behoben.** Anlass: Der Random-Seed-Slider ließ einzelne Werte (z. B. 21) per Drag nicht einstellen. Befund und Fixes (jeweils synchron in `app/src/*.jsx`, `app/rubin_ui_src.jsx`, `app/frontend/index.html`): **(1) Seed-Präzision:** 1000 Stufen (0–999) auf halber Spaltenbreite ≈ 3 Werte/Pixel — exakte Werte per Maus praktisch unerreichbar. Das Wert-Badge neben „Random Seed" und „Tuning Seed" ist jetzt ein editierbares Zahlenfeld (0–999, geclampt/gerundet, synchron mit dem Slider); der Slider bleibt für Grobeinstellung. **(2) Seed 0 defekt (UI + Export):** Falsy-Muster `cfg.seed||42` ließ bei Seed 0 den Slider-Thumb auf 42 springen und — gravierender — exportierte `SEED: 42` statt `0` in die YAML (analog `tuning_seed`/18). Jetzt nullish-sicher (`==null`-Prüfung) in Slider-Value, `--pct` und `12_yaml_export.jsx`; Import (`07_yaml_parser.jsx`) übernahm 0 schon korrekt. **(3) Doppelter YAML-Key:** `feature_selection.max_features` wurde bei gesetztem Wert zweimal geschrieben (redundante zweite Zeile entfernt; PyYAML nahm still die letzte — gleicher Wert, aber unsauber). **(4) Nullwerte unsichtbar:** Die `Inp`-Komponente (`value={value||""}`) zeigte 0 als leeres Feld — betroffen u. a. `reference_group: 0` (der Multi-Treatment-Standardfall!), „Max. Prediction-Rows (0=alle)", Timeouts. Jetzt `value===0?0:(value||"")`. **(5) Step-Raster-Fehlausrichtung (11 Suchraum-Slider):** Bei den Range-Slidern der Tuning-Suchräume lagen `max` und die Defaults (`dLow`/`dHigh`) nicht auf dem `min + k·step`-Raster — z. B. `learning_rate` (min 0.001, step 0.005): max 0.3 endete bei 0.296, Default 0.15 war unerreichbar. Steps korrigiert auf den gröbsten Wert, der min→max und beide Defaults exakt trifft (`learning_rate` 0.005→0.001 (4×), `min_child_weight` 0.5→0.001 bzw. 0.1, `l2_leaf_reg` 0.5→0.1 bzw. 1→0.5, `random_strength` 0.1→0.001 bzw. 0.5→0.1, `model_size_reg` 0.5→0.1); ein Checker-Skript bestätigt 0 verbleibende Raster-Fehler. Geprüft und in Ordnung: alle übrigen `Sld`-Raster (Korrelation, dfFrac, Penalties/Toleranzen inkl. Preset-Werte 0.2/0.35, Surrogate, SHAP), Export-Default-Guards gegen geleerte Felder (`Number("")→0` wird von `||default`-Guards abgefangen), `dml_crossfit_folds`-Guard, mcIters-Gating, RSlider-Low/High-Kollisionsschutz, vollständiges Parser-Mapping aller Slider-Felder.
- **Gebündeltes Ensemble in Production unbrauchbar (`'CausalForestDML' object has no attribute '_d_t_in'`) — behoben durch Full-Data-Refit ALLER Modelle beim Bundle-Export (Verhaltensänderung).** Ursache: Im CV-Modus fittet `train_and_crosspredict_bt_bo` nur Fold-Deepcopies (`_fit_single_fold` → `copy.deepcopy(...).fit(...)`); das Registry-Objekt `models[name]` bleibt ungefittet (ohne `_d_t_in`). Beim Export wurden bislang nur der Champion (auf vollen Daten) und Challenger (auf Trainingsdaten) refittet, das `EnsembleCateEstimator` aber nur dann mit refitteten Mitgliedern neu gebaut, wenn das **Ensemble selbst Champion** war. Bei einem Nicht-Ensemble-Champion mit `include_challengers: true` referenzierte die gebündelte `Ensemble.pkl` daher die ungefitteten Original-Mitglieder → `const_marginal_effect` warf in Production. Im Analyse-Lauf fiel das nie auf, weil Ensemble-Metriken durch Mitteln der OOF-Prediction-DataFrames entstehen, nicht durch einen Aufruf des Objekts. **Neu** (`_run_bundle_export` in `analysis_pipeline.py`): Bei `selection.refit_champion_on_full_data: true` (Standard) werden **alle** trainierten Modelle auf vollen Daten refittet und das Ensemble **immer** (nicht nur als Champion) self-contained aus den refitteten Mitgliedern neu erstellt — EconML serialisiert die Mitglieder in die Ensemble-Pickle, womit das Ensemble unabhängig von den Standalone-Dateien mit garantiert gefitteten Mitgliedern läuft. Damit ist in Production zudem **jedes** Modell frei wählbar; `include_challengers: false` wird in diesem Modus bewusst übergangen (Log-Hinweis). Im **Holdout-Modus** unterbleibt der Voll-Refit (Modelle bleiben auf Train gefittet), um die Evaluation nicht zu entwerten. Nicht-fit-fähige Mitglieder werden defensiv ausgelassen und die Ensemble-Gleichgewichtung neu normiert. Die vierfach verzweigte Refit-Schleife wurde durch eine einheitliche `_refit_one`-Hilfsfunktion ersetzt. Neue Metadaten-Flag `refit_all_on_full_data`. **Konsistent propagiert:** Settings-Felddoku (`settings.py`), Logging (Schritt-1/2-Meldungen), `docs/architektur.md` (Schritt 5b + 8), `docs/konfiguration.md`, `docs/bundles.md`, `rubin_overview.html` (4 Stellen), UI-Toggle „Alle Modelle auf gesamtem Datensatz refitten" (Label + Hilfetext in `app/src/11_page_tuning.jsx`, `app/rubin_ui_src.jsx`, `app/frontend/index.html`). Die Bedeutung von `refit_champion_on_full_data` ist damit von „nur Champion" auf „alle Modelle" erweitert; der Feldname bleibt (keine Schema-Migration). Neuer Regressionstest `tests/test_bundle_ensemble_refit.py` (3 Tests: gebündeltes Ensemble scort ohne `_d_t_in`, Mitglieder tragen den Fitted-Marker, alle Modelle exportiert + Nicht-Ensemble-Champion bleibt scorebar).

### Behoben
- **`pixi run -e prod score` wäre fehlgeschlagen: Tasks sind feature-gebunden.** Der `[tasks]`-Block gehört zum default-Feature — mit `no-default-feature = true` hatte das prod-Environment **keinen einzigen Task** (das Repo nutzt für app/dev bereits eigene Task-Tabellen, prod fehlte). Neu: `[feature.prod.tasks]` mit `score` (CLI-Scoring) und `scoring` (Produktivsetzung via `production/run_scoring.py`); `scoring` zusätzlich im default-`[tasks]` für Entwicklung/Tests. Die dokumentierten Kommandos (`pixi run -e prod score`/`scoring`) funktionieren damit wie beschrieben. (Empirischer pixi-Lauf hier nicht möglich — Binary-Download 403; Semantik durch pixi-Doku und die repo-eigene Feature-Task-Struktur belegt.)
- **Shell-Skript: `--frozen` hätte mangels Lockfile sofort abgebrochen.** `pixi.lock` ist im Repo bewusst gitignored (Zeile 25) und war auch im Original nie enthalten — `pixi install --frozen` schlägt ohne Lockfile fehl. Das Job-Skript prüft jetzt: Lockfile vorhanden → `--frozen` (deterministisch); sonst regulärer Install mit Log-Warnung und Empfehlung, `pixi.lock` für Prod-Reproduzierbarkeit zu committen (bewusste Team-Entscheidung, nicht eigenmächtig geändert).

### Hinzugefügt
- **UI-Synchronitäts-Checker `scripts/check_ui_sync.py`** (+ dev-Task `ui-sync`): beweist die Drei-Ebenen-Konsistenz der UI (`app/src/*.jsx` ↔ `rubin_ui_src.jsx` ↔ `frontend/index.html`) byte-genau und meldet bei Drift die erste Divergenz-Position. Hintergrund: Es gibt keinen automatisierten Bundle-Build — die drei Kopien werden per Konvention manuell synchron gehalten (`docs/app_build.md`, dort jetzt verlinkt), was eine reale Drift-Quelle ist. Positiv- und Negativtest (künstlicher Drift → Exit 1) verifiziert.

### Geändert
- **Doku-Tiefenprüfung: `rubin_overview.html` und restliche Dokumente auf den aktuellen Stand gebracht.** Die Overview beschrieb das Ensemble noch an 7 Stellen als EconML-`EnsembleCateEstimator` (Modelltabelle, Config-Tabelle, Ensemble-Details, SHAP-Hinweise, Bundle-Trainingsstand) — alle auf `ShapeSafeEnsembleCate` umgestellt, inkl. Begründung (shape-normalisierte Mittelung, self-contained) und der Korrektur „Ensemble wird beim Export *immer* neu gebaut" (statt nur als Champion). Neu in der Overview: Details-Block **„Produktivsetzung: production/run_scoring.py"** in der Production-Sektion (Config-getriebenes Scoring, XPT-V5-Namenskürzung, Monitoring-JSON mit −1-Raten, Surrogate optional, Domino-Job mit `--frozen`/prod-Env), `ml_package_versions`-Stempel in der Bundle-Dateitabelle, prod-Env-Hinweis in der Kommando-Tabelle. Der repo-weite Sweep fand darüber hinaus 7 weitere veraltete Klassen-Referenzen außerhalb der Overview (`architektur.md` 2×, `konfiguration.md` 2×, `bundles.md`, `explainability.md`, Registry-Docstring, ein Code-Kommentar) — alle nachgezogen; verbleibende Erwähnungen sind ausschließlich bewusste Erklär-/Test-Referenzen. HTML-Tag-Balance der Overview verifiziert (alle 7 Tag-Typen ausgeglichen); Binär-Scoring-E2E nach den Review-Guards erneut real gefahren (unverändert korrekt). Suite: 203/203.
### Hinzugefügt
- **Finaler Review-Nachtrag Production-Scoring.** Zwei Robustheits-Guards in `run_scoring.py`: leerer Input (0 Zeilen) und ID-Spalten, die zugleich Modell-Features sind (hätte doppelte Output-Spalten erzeugt), brechen jetzt mit klarer Meldung ab (beide negativ getestet). Shell-Skript: SSH-Key-Handling toleriert abweichende Key-Namen des Service-Users (`id_ed25519`/`id_ecdsa`; Warnung statt Abbruch, wenn keiner liegt — der Clone liefert dann die klare Meldung). Zwei bisher ungetestete reale Pfade verifiziert: **Multi-Treatment-Scoring** (Arm-Expansion `SCORE_P→SCORE_P1/P2`, `SCORE_B→SCORE_B1/B2` inkl. `column_order`-Expansion und V5-tauglicher ≤8-Zeichen-Namen; XPT + Monitoring geprüft) und der **`csv_path`-Parallel-Output** (ungekürzte Spaltennamen). Haupt-README verweist jetzt auf `production/`; Config-Kommentar stellt klar, dass die `TIMESTAMP`-Spalte automatisch gesetzt wird. Suite: 203/203.
### Hinzugefügt
- **Domino-Job-Skript `production/run_scoring_ph.sh`** (Master versioniert im Repo; die ausgeführte Kopie liegt auf dem Domino File System als Job-Einstieg). Modernisiert gegenüber dem bisherigen Muster: `set -euo pipefail` (das Alt-Skript lief nach gescheitertem Clone stumm weiter und hätte einen alten Stand gescort), frischer Clone mit pinnbarem Stand (`GIT_REF`, für Prod-Läufe Tag/Commit empfohlen; Fallback voller Clone + Detached-Checkout für SHAs), TLS-Variablen für pixi/uv explizit gesetzt (auf Job-Executors ist die `.devboxrc` nicht garantiert), **`pixi install --frozen -e prod`** statt `pixi install` + `pixi add pyreadstat`/`seaborn` — die `pixi add`-Zeilen entfallen ersatzlos (pyreadstat ist Bestandteil der pixi.toml, seaborn diente nur den durch das Monitoring-JSON ersetzten Notebook-Plots; `pixi add` auf dem Executor mutierte die pixi.toml und triggerte nicht-deterministische Re-Solves). Parametrierbar per Env (`GIT_REF`, `SCORING_CONFIG`, `BUNDLE_/INPUT_/OUTPUT_OVERRIDE`), Service-User-tauglich: keine Git-Identity mehr (nur für Commits nötig), nur SSH-Deploy-Key; Laufzeit-Log, Fehler propagieren als roter Domino-Job. Verifiziert: `bash -n`, empirischer Trockenlauf der Override-/`set -e`-Logik (leere Overrides brechen nicht ab); shellcheck war in der Prüfumgebung nicht verfügbar.
- **Generisches Production-Scoring `production/run_scoring.py` + Use-Case-Configs — ersetzt die früheren Produktions-Notebooks.** Neuer Repo-Ordner `production/` mit dem Config-getriebenen Skript (`--config production/scoring_ph.yml`, CLI-Overrides für `--input/--bundle/--output`), der PH-Referenz-Config (Werte des Alt-Notebooks: sas7bdat-Chunked-Read ISO-8859-1, IDs `PARTNER_ID_V`/`GESELLSCHAFT_FS`, Meta-Spalten `SCORE_TYP`/`VALID_TO`/`SCORE_VERFAHREN`, Timestamp `YYYY-DDD`, `table_name kau_ph`, XPT **V5** als Vorgabe des Zielsystems) und einem README. **Was das Bundle-Konzept ersetzt:** die 4 losen Preprocessing-Artefakte + Hand-Encoding (→ `preprocessor.transform`), 7 Einzel-Pickles inkl. des von Hand nachgebauten NSdml (→ `ProductionPipeline` + shape-sicheres `predict_in_batches` über `_predict_effect`, MT-fähig via `SCORE_P1/P2/…`), die drei Alt-Ensembles (→ `Ensemble.pkl`). **Bewusst entfallen:** die Batch-Skalierung (`StandardScaler` wurde pro Lauf neu gefittet → Scores zwischen Läufen unvergleichbar) — nur noch Rundung (`round_decimals`, Default 6); fachliche Sonderregeln gehören in DataPrep (Grundsatz im README); generischer Ersatz der alten Inf-Einzelregel: optionales Inf→NaN vor `transform` (→ gelernte Imputation). **`SCORE_B` (Surrogate) ist optional** (fehlt der Baum im Bundle → Spalte entfällt mit Log statt Fehler). **XPT-V5-Transparenz:** die 8-Zeichen-Kürzung der Variablennamen (seit jeher so beim Zielsystem, z. B. `PARTNER_ID_V`→`PARTNER_`) wird jetzt explizit geloggt, ins Monitoring geschrieben und Kürzungs-**Kollisionen** werden hart abgelehnt. **Monitoring-JSON pro Lauf** (versioniert `<name>_<YYYY-DDD>_<HHMMSS>.json` neben der XPT in `monitoring/`, plus überschriebene `<name>_latest.json`; XPT selbst wird überschrieben — Historisierung macht das Zielsystem): Laufzeit, Input-Umfang, Bundle-Infos inkl. `ml_package_versions` + Laufzeit-Abweichungen, fehlende/verworfene Spalten, Inf-Zellen, **−1-Rate pro kategorialer Spalte** (Drift-Signal, Warnung bei >1 %), Score-Statistiken (min/p25/median/mean/p75/max/std/NaN) pro Spalte, NaN-Score-Warnung, Output-Details. **Verifiziert:** 3 neue Tests (`tests/test_run_scoring.py`: E2E mit Monitoring-Assertions, Surrogate-optional, unbekanntes Modell → klarer Fehler; V5-Kürzung und V8-Vollnamen je getestet) + realer CLI-Lauf gegen ein echtes Bundle (2500 Zeilen, XPT-Layout identisch zum Alt-Notebook, 6 s). Suite: 203/203.
- **SAS-Export-Fähigkeit im prod-Environment + Versions-Gleichstand zwischen Analyse- und Scoring-Environment abgesichert.** Drei zusammenhängende Änderungen: **(1) pyreadstat** (≥1.2) in die Haupt-Dependencies und ins `prod`-Feature aufgenommen — für den SAS-Export via `write_xport` (XPT V8, 32-Zeichen-Variablennamen); natives sas7bdat-Schreiben existiert in der Python-Welt nicht (pandas hat kein `to_sas`, `sas7bdat` ist read-only; einzige Alternative wäre `saspy`, das eine laufende SAS-Session voraussetzt). Smoke-Test: XPT-V8-Roundtrip (schreiben + zurücklesen) verifiziert. **(2) `prod`-Environment in die `default`-solve-group** verschoben (statt eigener Group): pixi garantiert damit **identische Versionen der gemeinsamen Pakete** (econml, lightgbm, catboost, scikit-learn, pandas, numpy) zwischen Analyse- und Scoring-Environment — mit getrennten Groups hätten die Environments bei einem `pixi update` auseinanderlaufen können, und Bundles sind Pickles (Entpickeln über econml-Versionsgrenzen ist riskant). **(3) Versions-Stempel + Lade-Warnung als Sicherheitsnetz:** `metadata.json` enthält jetzt `ml_package_versions` (Export-Zeitpunkt, via `importlib.metadata`); die `ProductionPipeline` lädt die Metadaten (`self.metadata`, neu), vergleicht beim Init gegen die Laufzeit und warnt bei Abweichungen mit Detail-Liste (`self.version_mismatches`, kein Abbruch; Alt-Bundles ohne Stempel bleiben still kompatibel). Neuer Test `test_bundle_records_and_checks_ml_package_versions` (Stempel korrekt, Warnung bei manipuliertem Stempel, Alt-Bundle-Kompatibilität). Suite: 200/200.
- **Schlankes `prod`-Pixi-Environment für Production-Scoring + Lazy-Imports in `rubin/__init__.py`.** Bisher zog bereits `from rubin.pipelines.production_pipeline import ProductionPipeline` die komplette Analyse-Kette (matplotlib, shap, econml-Tuning, …), weil das Paket-`__init__` eager `AnalysisPipeline` importierte — ein dediziertes Scoring-Environment war damit unmöglich. **(1) `rubin/__init__.py` auf PEP-562-Lazy-Imports umgestellt:** Alle 14 Top-Level-Symbole werden erst beim ersten Attributzugriff aufgelöst (mit Cache und `TYPE_CHECKING`-Block für IDEs); `from rubin import AnalysisPipeline` etc. funktioniert unverändert. Der Production-Import lädt jetzt nur noch sklearn/scipy (fürs Entpickeln ohnehin nötig) statt zusätzlich matplotlib/shap — Importzeit ~1,4 s statt ~3,5 s. **(2) Neues Environment in `pixi.toml`:** `[feature.prod]` mit den minimalen Laufzeit-Deps (numpy, pandas <3, pydantic, scikit-learn, lightgbm, econml, pyarrow, pyyaml; catboost + editierbares `rubin` via PyPI) und `prod = { features = ["prod"], no-default-feature = true, solve-group = "prod" }` — bewusst ohne mlflow/optuna/matplotlib/shap/flaml und mit eigener Solve-Group. Nutzung: `pixi run -e prod score -- --bundle … --x … --out …` (in README, `docs/README_DE.md`, `docs/bundles.md` dokumentiert). **Empirisch verifiziert:** volle Suite nach dem Lazy-Umbau grün (196/196); End-to-End-Simulation — Bundle mit vollem Stack exportiert, dann in einem Prozess mit per Import-Hook **blockierten** mlflow/optuna/matplotlib/shap/flaml/flask geladen und Champion + Ensemble erfolgreich gescort (endliche CATEs, 300 Zeilen). Damit ist belegt, dass das prod-Dependency-Set für Laden + Scoring vollständig ist.
- **Heterogenitäts-Bewertung dokumentiert.** Die exakte Klassifikationslogik der Report-Sektion „Heterogenität" (`_compute_heterogeneity_assessment`) — Eingangsgrößen, Konzentrations-Definition (`uplift_at_10pct/uplift_at_50pct`, =0 falls einer ≤0), die Schwellen für stark/moderat/schwach/keine (`n_positive_qini` ≥3/≥2, `champ_pv`, `concentration` ≥1.5) inkl. Zahlenbeispiel sowie der separate historische Score-Vergleich — ist jetzt in `docs/evaluation.md` (Abschnitt „Heterogenitäts-Bewertung im Report") und als ausklappbarer Detailblock in `rubin_overview.html` (Sektion 10 Evaluation) festgehalten. Reine Doku, kein Code-/Verhaltens-Change.
- **Ausgewählte Features nach der Feature-Reduktion wegloggen (inkl. Datentyp).** `_run_feature_selection` (`analysis_pipeline.py`) loggte bisher nur Anzahlen (`8 → 4 Features (Korrelation: −1, Importance: −3)`), nicht die tatsächlich behaltenen Feature-Namen. Neu: nach abgeschlossener Reduktion (sobald Importance-Selektion oder Korrelationsfilter lief) wird die finale Feature-Menge namentlich **mit pandas-Datentyp** als INFO-Log ausgegeben (`Feature-Selektion: N ausgewählte Features nach Reduktion: feat (float64), …`) und — symmetrisch zu den bestehenden `removed_features_corr.json`/`removed_features_imp.json`-Artefakten — als `selected_features.json` (Liste von `{"feature", "dtype"}`) ins MLflow-Run geloggt. So sind z. B. kategorische Spalten (`category`/`object`) direkt erkennbar. Reine Beobachtbarkeit; verändert die Selektion selbst nicht.
- **`generate_html_report` strukturell aufgeteilt (reiner Refactor, byte-identischer Output).** Der 1077-Zeilen-Monolith wurde in 14 Sektions-Builder-Funktionen auf Modulebene zerlegt (`_render_summary_bar`, `_render_overview`, `_render_heterogeneity` (+ Helper `_assessment_card`/`_HET_SYM`), `_render_data`, `_render_dataprep`, `_render_feature_selection`, `_render_tuning`, `_render_fmt`, `_render_cft`, `_render_comparison`, `_render_model_detail`, `_render_surrogate`, `_render_explainability`, `_render_timing`). `generate_html_report` ist jetzt ein schlanker Orchestrator (0 verbliebene Inline-`h +=`) und behält ausschließlich die Steuerlogik: geteilter Kontext (`champ`/`sel_met`/`is_external`/`_is_tmes`/`higher`/`_has_modeling`), Sektions-Reihenfolge, `_sec`/`_nav_label`-Aufrufe, Per-Modell-Schleife inkl. `_plot_order`, sowie Assembly + statisches Template/CSS. Die f-String-**Inhalte** wurden verbatim verschoben (nur Python-Einrückung und Variablen-Übergabe geändert), `ReportCollector` unangetastet. **Verifikation:** Der Golden-Master-Test blieb nach *jeder* der 14 Extraktionen byte-identisch; zusätzlich ein Cross-Check, der 6 Fixture-Varianten (Hauptpfad, external, TMES, Beobachtungsdaten inkl. No-Confounding-Warnung, non-`both`/Single-Fold, FMT-`best_scores`, Minimalfall) durch die Pre-Refactor- *und* die refaktorierte Version rendert: die einzigen Unterschiede sind die bereits dokumentierten früheren Inhaltsänderungen (FS-Text, „BLT-Fits", Fit-Budget-Box, „Cap"-Anzeige, CFT-Fits-Karte) in genau FS/Tuning/FMT/CFT — **alle übrigen Sektionen byte-identisch**. Zweiter dauerhafter Golden-Snapshot (`report_golden_external.html`, external + FMT-`best_scores`) ergänzt, der die von der ersten Fixture nicht abgedeckten Zweige absichert (jetzt 3 Tests im Golden-Modul).
- **Golden-Master-Regressionstest für den HTML-Report (`tests/test_report_golden.py` + `tests/data/report_golden.html`).** Erster Test für den Report-Generator überhaupt. Eine voll befüllte Fixture löst alle 12 Sektionen (+ Per-Modell-Sektionen + beide Overfit-Penalty-Karten) aus; das gerenderte HTML wird nach Maskierung des einzigen nicht-deterministischen Elements (Timestamp) byte-genau gegen einen eingefrorenen Snapshot verglichen. Plots sind feste Fake-base64-Strings → vollständig deterministisch ohne matplotlib. Dient als Sicherheitsnetz für eine geplante strukturelle Überarbeitung von `generate_html_report` (Aufteilung des 1077-Zeilen-Monolithen in Sektions-Builder bei byte-identischem Output) und als dauerhafter Regressionsschutz. Golden neu erzeugbar via `python tests/test_report_golden.py --update`.
- **Saturierender Cap für die Train-Val-Gap-Penalty (`overfit_max_penalized_gap`, Verhaltensänderung).** Die Overfit-Penalty war nach oben unbeschränkt: Über `relative_gap = (train − val) / max(|val|, 1e-8)` explodierte sie bei kleinen Val-Scores (Qini ~1e-4, R-Score nahe 0) und konnte den Abzug so groß machen, dass er das Vorzeichen kippte (z.B. val=0.01, train=0.06, penalty=0.3 → adjusted=−0.005) und die Selektion vom Gap-Minimieren statt von der Val-Qualität dominiert wurde — was tendenziell das am stärksten unterangepasste Modell bevorzugt und die finale Qini drückt. Neu wird der bestrafte Exzess-Gap gedeckelt: `excess = min(max(0, relative_gap − tolerance), max_penalized_gap)`, d.h. der maximale Abzug ist `penalty × scale × max_penalized_gap`. Mit Default `1.0` und `penalty ≤ 1` kann das Vorzeichen nicht mehr kippen; jenseits des Caps saturiert der Abzug. `<=0` deaktiviert den Cap (= altes unbeschränktes Verhalten). Das Feld ist in alle drei Tuning-Configs aufgenommen (`tuning`, `final_model_tuning`, `causal_forest`, jeweils Default 1.0), in allen drei Tunern (`base_learner`/`final_model`/`causal_forest`) implementiert (BLT/FMT lösen aus der Config auf, CFT-Static nimmt den Wert als Parameter), in `config_reference_all_options.yml`, UI (`DEFAULT_CFG` + YAML-Parser/-Export, Bundle neu gebaut), HTML-Report (Anzeige „Cap N"/„kein Cap"), `tuning_optuna.md`, `konfiguration.md` und `rubin_overview.html` dokumentiert. **Hinweis:** Dies verändert die bestrafte Score-Selektion und damit die getunten Hyperparameter — eine bewusste Verhaltensänderung, nicht byte-reproduzierbar. Neue Tests (`TestOverfitPenaltyCap`, 6 Stück) sichern Sign-Flip-Verhinderung, Saturierung, Deaktivierung (= Altverhalten), Config-Auflösung und den statischen CFT-Pfad. Suite: 162 passed.
- **Fit-Budget im Report ehrlich gemacht:** Die BLT-Karte „Modell-Fits gesamt" hieß irreführend „gesamt", zählte aber nur Nuisance-Einzelfits des Base-Learner-Tunings → umbenannt zu „BLT-Fits (nur Nuisance-Tuning)". Neu: eine „Fit-Budget gesamt (Schätzung)"-Box, die zusätzlich die tatsächlichen Top-Level-Modell-Fits des Trainings (Cross-Prediction: `N × (K + 1)` bzw. extern `N × 1`) plus Bundle-Refits ausweist und darauf hinweist, dass jeder DML-Fit intern ~`mc × dml_crossfit_folds × 2 + 1` Sub-Fits auslöst (Einzel- und Top-Level-Fits sind nicht summierbar). Außerdem zeigt die CFT-Sektion jetzt — analog zu FMT — eine Fit-Schätzung (`Folds × (Pre-Fit + Scorer) + Trials × refit_final`). Kommentar in `training.py` ergänzt, warum `m_full` (Full-Data-Fit für die `Train_`-Spalte) bewusst gelöscht und das exportierte Modell am Ende neu gefittet wird (Memory-vs-Compute-Tradeoff).

- **drtester_plots Split:** `drtester_plots.py` (1385L) aufgeteilt in 3 Module: `drtester_core.py` (DRTester-Klassen + Nuisance), `evaluation_plots.py` (CATE/Uplift-Plots), `score_plots.py` (Qini-Vergleiche + Score-Redistribution). Backward-kompatibler Re-Export-Shim.
- **server.py Blueprint-Split:** `server.py` (1327L) aufgeteilt in App-Factory + `app/state.py` (Shared State) + 6 Blueprint-Module in `app/routes/` (frontend, health, data, config, analysis, results). Keine Datei über 412 Zeilen.
- **UI Datei-Split:** `rubin_ui_src.jsx` (3975L) aufgeteilt in 14 JSX-Quelldateien in `app/src/`. Auto-Build via `build_ui.py`. Konstanten mehrzeilig formatiert.
- **Overfit-Penalty skaleninvariant:** Floor von 0.1 auf 1e-8 geändert. Penalty funktioniert jetzt identisch für R-Score (~0.3) und Qini (~0.0001). Train-Qini-Berechnung in QiniScorer-Pfad (FMT + CFT): `effect(X_train)` pro Fold, In-Sample-Qini vs OOF-Qini Gap-Penalty. `CausalForestConfig` um `overfit_penalty` + `overfit_tolerance` erweitert.
- **QiniScorer für FMT + CFT:** Neue Scorer-Option `scorer: qini` als Alternative zum EconML RScorer. Default bei RCT via `scorer: auto`. Optimiert Ranking-Qualität (OOF-Qini) statt pointwise CATE-MSE. Kein Pruning, kein RScorer-Cache. Robust bei schwachem Signal (Mahajan et al. 2023). Config: `final_model_tuning.scorer` / `causal_forest.scorer` (auto | qini | rscore).
- **Tuning-Modul-Split:** `tuning_optuna.py` (2412 Zeilen) → `rubin/tuning/` Paket mit 4 Modulen: `common.py` (Builder, Search-Space, Utilities), `base_learner.py` (BaseLearnerTuner), `final_model.py` (FinalModelTuner), `causal_forest.py` (CausalForestTuner). Keine Backward-Compat-Shim — alle Imports direkt auf neue Pfade.
- **CausalForestTuner Klasse:** `tune_causal_forest()` Standalone-Funktion → `CausalForestTuner` Klasse mit internem RScorer-Cache (`self._scorer_cache`). Bei `scorer: rscore` werden Scorer zwischen CausalForestDML- und CausalForest-Calls wiederverwendet. Bei `scorer: qini` wird kein Cache benötigt.
- **Overfit-Penalty Scale Floor:** `scale = max(|val_score|, 0.1)` statt `max(|val_score|, 1e-10)`. Verhindert disproportionale Penalty bei val_score ≈ 0 (z.B. R-Score nahe Null bei schwachem CATE-Signal).
- **`store_fold_aligned_data()` / `get_fold_aligned_data()` Helpers:** Abstraktion über `DataFrame.attrs` mit Modul-Cache-Fallback + Warnung bei attrs-Verlust.
- **`_predict_effect` Squeeze Safety:** Blanket `squeeze()` → gezielter Trailing-Dim-Squeeze. Schützt MT-K-1-Dimension bei `(n, K-1)` Shapes.
- **`max_features` Default: 50 → 77** in Settings, Feature Selection, HTML Report, 3 Configs, JSX/HTML (17 Stellen, 8 Dateien).
- **TunedSet/TuningTask Docstrings** ergänzt.

- **Dual-Seed-System:** Separater `tuning_seed` (Default: 18) für Tuning-CV-Folds, verhindert Val-Set-Overfitting. Cross-Prediction nutzt `SEED` (42), Tuning nutzt `tuning_seed` (18). UI-Slider + Warnung bei gleichen Seeds. Theoretische Einordnung (vs. Nested CV) dokumentiert.
- **Produktions-Datenregime:** BLT-Tasks für DML/DR-Nuisance subsamplen die Trainingsmenge auf (K-1)/K, um das DML-interne Cross-Fitting zu simulieren. Scopes bleiben getrennt (7 Tasks).
- **RAM-Monitoring:** `_log_ram()` an 8 Phasen-Übergängen mit Warnung ab 80% Auslastung.
- **FMT Crash-Resilienz:** try/except pro FMT-Modell — DRLearner-Crash verliert nicht mehr NonParamDML-Ergebnisse.
- **Pipeline-Log Validierungsmodus:** Expliziter Log nach Pipeline-Start (Cross-Val mit Seeds / External / TMES).
- **Skill Scores (BLT):** Verbesserung gegenüber naivem Baseline-Modell. Klassifikation: `Skill = 1 − log_loss / baseline_log_loss`. Regression: `R² = 1 − MSE / Var(Y)`. Geloggt in Logger, MLflow, JSON-Artefakt, HTML-Report (neue Spalte).
- **ATE-Barplot:** Response Rates pro Treatment-Gruppe mit SE-Fehlerbalken, ATE-Pfeil (BT), Δ-Labels (MT). Im Report unter "Datengrundlage".
- **Native Uplift-Plots:** Qini-Kurve (+ Perfect-Kurve, name-Parameter), Uplift-by-Percentile (+ strategy/kind/string_percentiles), Treatment-Balance (+ random-Parameter). 
- **CausalForest-Tuning (CFT):** Optuna TPE über 4 kausale Parameter (max_depth, min_weight_fraction_leaf, min_var_fraction_leaf, criterion). CausalForestDML via cache_values(), CausalForest via CausalForestAdapter. Scoring via QiniScorer (RCT) oder RScorer (obs.). Ersetzt EconML tune() Grid-Search.
- **Unified Tuning-Logging:** Alle 3 Wellen: konsistentes Log-Format pro Study.
- **`cache_values` + `refit_final()` (FMT + CFT):** Nuisance-Modelle werden einmalig pro äußerem Fold gecacht (EconML `cache_values=True`). Pro Trial wird nur `model_final` via `refit_final()` auf gecachten Residuals neu gefittet. Fit-Reduktion: 2.750 → 305 Fits (5F, 50T). Trials laufen sequentiell (`n_jobs=1`), alle CPU-Kerne pro Fit (`parallel_jobs=-1`).
- **`discrete_outcome=True` (DRLearner):** `model_regression` ist jetzt ein Classifier (EconML nutzt `predict_proba()` für E[Y|X,T]). Tuning-Signatur: `("outcome", "classifier")`. Konsistent in model_registry, tuning_optuna, UI und Doku.
- **`allow_missing=True`:** Auf alle EconML-Modelle gesetzt (außer CausalForestDML/CausalForest — GRF kann kein NaN in X). CatBoost (`nan_mode="Min"`) und LightGBM (`use_missing=True`) verarbeiten NaN nativ.

### Geändert
- **Treatment-Balance: Ziel-Rate nach effektiver Stichprobe statt nach minimalem Zeilenverlust (Verhaltensänderung).** `_check_and_balance_treatments` (`data_prep_pipeline.py`) wählte die gemeinsame Ziel-Treatment-Rate bisher als diejenige der beiden Kandidaten {min(Raten), max(Raten)}, die **am wenigsten Zeilen** verliert. Das maximiert die Rohzahl N, ignoriert aber die Balance und kann einen Arm stark schrumpfen lassen (schlechter Overlap, instabile CATE/Qini). Neu wird die Ziel-Rate nach **maximaler effektiver Stichprobe** `effektive N = N_keep · p·(1−p)` gewählt. Begründung (Heterogenitätsanalyse): Die Varianz eines Effekt-/Uplift-Schätzers skaliert mit `1/(N·p·(1−p))`, der kleinere Arm limitiert die Präzision — eine balanciertere Rate kann daher gewinnen, obwohl sie mehr Zeilen verliert (Beispiel 61/62/67/50%: Ziel 50% → effN 800 schlägt Ziel 67% → effN 792, trotz 20% vs. 10,5% Verlust). Zusätzlich neue, optionale und per Default deaktivierte Schranken in `DataPrepConfig`: `balance_min_arm_abs`/`balance_min_arm_frac` (harte Mindestschranke je Arm — disqualifiziert Kandidaten, sonst bestes effektives N + Warnung), `balance_max_loss_frac` (Verlust-Deckel) und `balance_target_grid_step` (zusätzliche Zwischen-Zielraten Richtung 0.5; findet z. B. im Beispiel 60% mit effN 859 ≈ 0.215·N — höher als beide Endpunkte 800/792 — bei ≈10,5% Verlust). Die Downsampling-Mechanik selbst (per-Datei-Formeln, deterministisch per Seed) ist unverändert; Simulation und Ausführung nutzen identische Formeln. Logging weist jetzt alle Kandidaten mit effektiver N / kleinstem Arm / Verlust aus; MLflow zusätzlich `treatment_balance_effective_n` + `treatment_balance_min_arm`. Doku durchgängig aktualisiert (`evaluation.md`, `konfiguration.md`, `config_reference_all_options.yml`, `architektur.md`, `rubin_overview.html`). Neuer Test `tests/test_treatment_balance.py` (5 Stück: effektive-N-Auswahl, Single-File/balancierte No-Ops, Arm-Floor-Warnpfad, Gitter schlägt beide Endpunkte).

- **BLT-Overfit-Penalty greift nur noch für Meta-Learner; DML/DR-Nuisances werden nie bestraft.** Bisher wurde `tuning.overfit_penalty` im Base-Learner-Tuning uniform auf alle Tasks angewandt. Neu: die Penalty wirkt ausschließlich auf Meta-Learner-Tasks (S-/T-/X-Learner; `sample_scope ∈ {all_direct, group_specific_shared_params}`), während DML/DR-Nuisances (`sample_scope == "all"`, laufen im internen Cross-Fitting mit Neyman-orthogonalem Score) **nie** bestraft werden — unabhängig von der Konfiguration. Begründung: Cross-Fitting + Orthogonalität puffern Nuisance-Overfitting bereits ab (Chernozhukov et al. 2018, Bach et al. 2024); Meta-Learner-Modelle bilden den CATE dagegen direkt ohne diesen Puffer, weshalb die Penalty dort der primäre (und einzige, da kein FMT/CFT) Overfitting-Hebel beim Tuning ist. Umsetzung via neuem `_penalty_applies(task)` in `base_learner.py` + `allow_penalty`-Gate in allen vier BLT-Objectives; `_apply_overfit_penalty` selbst unverändert. Neuer Regressionstest `TestPenaltyAppliesMetaOnly`.
- **Toleranzen/Penaltys moderat entschärft + Reg-Presets setzen milde Meta-Learner-BLT-Penalty.** Aufbauend auf dem Meta-Learner-Gating: BLT-Toleranz-Default 0.15 → 0.20, FMT/CFT-Toleranz-Default 0.05 → 0.10 (Penalty-Defaults bleiben 0). Die Regularisierungs-Presets setzen jetzt zusätzlich eine milde Meta-Learner-BLT-Penalty (Moderat 0.2, Stark 0.3) — dank Gating nur S/T/X-Learner betroffen, DML/DR nie — sowie entschärfte Finalstufen-Penaltys (Moderat FMT/CFT 0.3 → 0.2, Stark 0.5 → 0.35; Toleranz je 0.10). Betroffen: `settings.py` (3 Tuning-Configs + Kommentare), UI (`DEFAULT_CFG`, Reg-Presets in `03_presets.jsx` + `09_page_config.jsx` inkl. methodischem Info-Block, Penalty-Hilfetexte + stale Preset-Erkennung in `11_page_tuning.jsx`; Bundle neu gebaut), HTML-Report (BLT-Penalty-Karte → „Overfit-Penalty (Meta-Learner)", Toleranz-Fallbacks 0.20/0.10; Golden-Snapshots regeneriert), sowie die gesamte Doku (`tuning_optuna.md`, `konfiguration.md`, `config_reference_all_options.yml`, `architektur.md`, `README_DE.md`, `developer_guide.md`, `rubin_overview.html`) durchgängig auf „BLT-Penalty nur Meta-Learner; DML/DR nie" + die neuen Werte umgestellt. Toleranz-Default-Tests angepasst (`test_blt_tolerance_default` → 0.20, `test_fmt_tolerance_default` → 0.10). Reine Default-/Preset-/Text-Änderung; die Penalty-Formel und das Gating selbst unverändert.

- **Base-Learner Default:** LightGBM → CatBoost (`base_learner.type: "catboost"`).
- **Random Seed Default:** 42 → 18 für tuning_seed (SEED bleibt 42).
- **CausalForest-Tuning:** EconML tune() Grid-Search (12/48 Kombinationen) → Optuna TPE (8-9 kontinuierliche Parameter).
- **FMT/CFT Scoring:** Umstellung von rohem MSE auf R-Score (1 − MSE(heterogen) / MSE(konstant)). Verhindert Intercept-Kollaps bei CausalForestDML und verbessert Metrik-Vergleichbarkeit.
- **FMT nutzt volle Trainingsdaten:** 80%-Subsetting entfernt — Dual-Seed-Schutz ist ausreichend.
- **DRLearner Nuisance-Count:** `mc*cv*(1+K)+1` → `mc*cv*2+1` (1× model_propensity + 1× model_regression pro Fold, nicht K×). Identisch mit NonParamDML.
- **FMT/CFT Parallelisierung:** Parallel (`n_cpus // 8`) → sequentiell (`n_jobs=1`, `cache_values`). Alle CPU-Kerne pro Fit (`parallel_jobs=-1`).
- **FMT UI:** Wellen-Steuerung → direkte Trial-Auswahl (30/50/100). Konsistent mit CFT.
- **Tuning-Defaults:** Alle 3 Stufen (BLT, FMT, CFT) sind per Default deaktiviert (`enabled: false`).
- **CFDML Parallelisierung:** `parallel_jobs=1` → `-1` für Nuisance-Modelle (Bug Fix — Cross-Fitting ist sequentiell, keine Übersubskription).
- **GC-Sweeps:** `gc.collect()` + `del est` / `del study` an allen Phasen-Übergängen.

### Gefixt
- **Score-Redistribution-Plot: rangbasierte Dezile statt wertbasierter Perzentile.** Die Dezil-Zuordnung (`_assign_bins` in `rubin/evaluation/score_plots.py`) nutzte `np.percentile` + `np.digitize`, was bei Bindungen im historischen Score (viele Nullen, diskrete/gerundete Werte, geclumpte Scores) zusammenfallende Perzentil-Kanten erzeugt und alle gleichen Werte in *ein* Dezil wirft — einzelne Hist-Dezile liefen über, andere blieben leer (empirisch: 35 % Nullen → ein Dezil 3755 statt 1000, Nachbardezile 0). Das verzerrte den gestapelten Plot und widersprach der Diagonal-Interpretation sowie dem bereits rangbasierten Spearman-ρ. Neu rangbasiert (Position in der stabilen Sortierung, `(rank·n)//N`): jedes Dezil enthält exakt ~N/n Samples, auch bei Ties (bei N nicht durch n teilbar Abweichung ≤ 1). Empirisch verifiziert über kontinuierliche, diskrete, gerundete und null-lastige Scores; echter matplotlib-Render fehlerfrei. Betrifft nur die Visualisierung (beide Achsen), keine Metrik/Selektion. Suite: 192 passed.
- **Champion-Callout zeigte bei manuellem Champion ein falsches Vorzeichen/Label.** Der Vorsprung des Champions gegenüber dem Zweitplatzierten wurde immer als `+|Δ|` mit Label „Zweitplatziert" angezeigt. Bei einem manuell gewählten Champion (`selection.manual_champion`), der nicht das metrisch beste Modell ist, ist das „beste Nicht-Champion-Modell" aber tatsächlich das Spitzenmodell — dann waren sowohl das `+`-Vorzeichen als auch „Zweitplatziert" falsch (der Champion liegt zurück). Jetzt vorzeichen-bewusst: Champion führt → `+Δ vs. X (Zweitplatziert)`; Champion liegt zurück → `−Δ vs. X (bestes Modell — manuell gewählter Champion)`. Verifiziert: `choose_champion` (Richtung), Vergleichstabellen-Sortierung/Ranking/Best-Value-Markierung und Heterogenitäts-Vergleich (`qini_diff = champ − hist`) sind sign-korrekt und konsistent. Golden unverändert (Standardfall: Champion = metrisch bestes). Suite: 192 passed.
- **Report-Config-Parität: CFT-Overfit-Penalty und `mc_agg` wurden nicht angezeigt.** Zwei in der UI konfigurierbare und vom Code genutzte Einstellungen fehlten im Report: (a) der **CausalForest-Tuning-Overfit-Penalty** (`causal_forest.overfit_penalty/tolerance/max_penalized_gap`) wurde vom CFT-Tuner angewandt (`causal_forest.py`), aber — anders als bei BLT und FMT — nicht in der CFT-Report-Sektion gezeigt; jetzt analog dargestellt (nur bei Penalty > 0). (b) Die MC-Aggregationsmethode **`mc_agg`** (mean/median) wurde nicht neben den MC-Iterationen angezeigt; jetzt ergänzt, wenn `mc_iters > 1` (sonst irrelevant). Beide via `cs`-Dict nachgezogen. Reine Anzeige-/Vollständigkeitskorrektur. Golden aktualisiert (Fixture hat mc_iters=3). Suite: 192 passed.
- **XLearner-Pseudo-Outcome-Nuisance wurde in der Fit-Schätzung um 2 über-zählt.** Die Pseudo-Nuisance (μ₀, μ₁) wird einmalig über `cv_splits` Folds × 2 Modelle (Control + Treated) berechnet — also `cv_splits × 2` Fits. Report (`_blt_fits_per_task`) und UI (`estimateFits`, `pseudoNuisanceOnce`, Labels) rechneten fälschlich `cv_splits × 2 + 2`; der „+2" entsprach dem Degenerations-Fallback (`_build_xlearner_pseudo_outcomes`, der übersprungene Folds ersetzt, **nicht** additiv hinzukommt). **Empirisch verifiziert** durch Instrumentierung der echten `LGBMClassifier/Regressor.fit`-Aufrufe in `tune_all`: gezählte Fits == Report-Formel == UI-Formel für SLearner/TLearner/NonParamDML/DRLearner/XLearner und kombinierte Sets über mehrere `n_trials`/`cv`-Werte (XLearner vorher 26, jetzt 24 = empirisch korrekt). Korrigiert an allen 4 Stellen (1× Report, 3× UI) inkl. Kommentare/Erklärtexte; UI-Bundle neu gebaut. Suite: 192 passed.
- **„Fit-Budget gesamt"-Box im Report zählte den Bundle-Refit auch ohne aktiven Bundle-Export.** Die Top-Level-Fit-Schätzung in der BLT-Sektion (`html_report.py`) addierte im Cross-Modus immer `n` Bundle-Refits; tatsächlich refittet die Pipeline nur bei aktivem Bundle-Export alle Modelle, sonst nur den Champion (1). Jetzt konditional über `bundle_enabled` (Bundle aktiv → `n` Bundle-Refit, sonst `1` Champion-Refit) — konsistent zur Pipeline und zur UI-Schätzung. Reine Anzeigekorrektur (Golden-Fixtures haben Bundle aktiv → unverändert). Suite: 192 passed.
- **Report-Tuning-Pläne (BLT & FMT) respektierten die Modell-Auswahl nicht.** Der Base-Learner-Tuning-Plan (`analysis_pipeline.py`) wurde aus `cfg.models.models_to_train` gebaut, der echte Tuner (`tune_all`) tunt aber nur `active_models` (gefiltert nach `cfg.tuning.models`); analog listete der FMT-Plan alle FMT-fähigen Modelle statt der per `cfg.final_model_tuning.models` ausgewählten (die echte FMT-Schleife filtert via `if fmt_models is not None and mname not in fmt_models: continue`). Bei Teilauswahl zeigte der Report dadurch Tuning-Tasks/Fits für Modelle, die gar nicht getunt wurden — inkonsistent zur UI (die `tuningModels`/`fmtModels` respektiert, leer = alle). Beide Pläne wenden jetzt denselben Auswahl-Filter an wie die echten Tuning-Schleifen. CFT respektierte `tune_models` bereits korrekt. Verifiziert: Report-BLT-Total (aus echtem `_build_plan(active_models)`) == UI-`estimateFits` über alle Modell-Teilmengen (RCT + Beobachtung). Reine Report-/Anzeigekorrektur. Suite: 192 passed.
- **CFT-Report zeigt jetzt beide getuneten Modelle (CausalForest und CausalForestDML) — analog FMT.** `add_cft_info` (`html_report.py`) überschrieb ein Einzel-Dict (`cft_info`) und wurde pro getuntem CF-Modell in der Schleife aufgerufen (`analysis_pipeline.py`), sodass nur das zuletzt verarbeitete Modell im Report erschien. Neu sammelt der Collector alle Ergebnisse in einer Liste (`cft_results`), und `_render_cft` rendert — wie die FMT-Sektion — eine Zeile pro Modell (Modell/Modus/Fold-Strategie/Trials/Berechnung/Fits + Gesamtzeile bei >1 Modell) sowie je eine Best-Score-Zeile pro Modell. `cft_info` bleibt als zuletzt gesetztes Dict für Abwärtskompatibilität (Golden-Fixture). Verifiziert: beide Modelle erscheinen mit korrekten Per-Modell-Fits (CausalForestDML 325 / CausalForest 270 bei cv=5, rscore).
- **Fit-Schätzungen im Analyse-Report und in der UI vereinheitlicht (CFT/FMT-Konsistenz).** Zwei Formel-Diskrepanzen behoben: (1) Der reine GRF (`CausalForest` via `CausalForestAdapter`) hat — anders als CFDML mit `cache_values` — **keinen** Forest-Pre-Fit pro Fold (der Adapter wird erst in den Trials gefittet, `causal_forest.py`); die Report-Schätzung zählte fälschlich einen (`per_fit=1` → jetzt `0`). (2) Die UI-Vorschauen (`FinalTuningPlanPreview`, CFT-Preview, Sidebar-Schätzung in `14_app.jsx`) hatten die RScorer-Fits (`4` pro Fold) hart kodiert und ignorierten den **QiniScorer** (0 Nuisance-Fits); jetzt qini/auto-bewusst (auto→qini bei RCT, →rscore bei Beobachtung) — synchron zur Report-Logik (`0 if qini else 4`). Damit liefern Report und UI über alle Scorer-/Fold-/Trial-Kombinationen identische Fit-Zahlen (BLT 120, FMT+CFT 32 Kombinationen verifiziert). UI-Bundle (`rubin_ui_src.jsx`, `frontend/index.html`) neu gebaut. Golden-Snapshots (CFT-Sektion: Per-Modell-Tabelle) regeneriert; Suite: 192 passed.
- **CFT-Fit-Schätzung im Analyse-Report nutzte die falsche CV-Größe für die äußeren Folds.** Die CausalForest-Tuning-Sektion (`_render_cft`, `html_report.py`) schätzte die äußeren OOF-Folds als `cross_validation_splits`, der CFT-Tuner (`causal_forest.py:140/200/224`) verwendet dafür aber `data_processing.dml_crossfit_folds` (dieselbe Zahl wie für das innere Cross-Fitting; das unterscheidet CFT von FMT, dessen äußere Folds tatsächlich `cross_validation_splits` sind). Da beide Werte in der UI getrennt einstellbar sind, war die geschätzte Fit-Zahl bei abweichenden Werten falsch. Jetzt nutzt `_cft_outer` ebenfalls `dml_crossfit_folds`. Verifiziert (cv_splits=5, dml_crossfit_folds=3 → äußere Folds 3 statt 5, Total 183). Reine Report-/Anzeigekorrektur. Nebenbei: toter `getattr(cfg.final_model_tuning, "cv_splits", 3)`-Fallback in der FMT-Plan-Berechnung (`analysis_pipeline.py`) auf `5` angeglichen (Feld-Default + Report-Fallback; das Feld existiert immer, der Fallback war unerreichbar). Golden-Snapshots unverändert (Fixture hat `cv_splits == dml_crossfit_folds`). Suite: 192 passed.
- **BLT-Fits-Berechnung im Analyse-Report korrigiert (task-abhängig) + veraltete „nur Nuisance"-Beschreibungen.** Die Base-Learner-Tuning-Sektion (`_render_tuning`, `html_report.py`) berechnete die BLT-Fits flach als `n_studies × n_trials × cv` und beschrieb BLT als reines Nuisance-Tuning. Beides war veraltet: (1) Die Fit-Zahl ignorierte, dass TLearner-(`grouped_outcome_regression`) und XLearner-(`pseudo_effect`)-Tasks pro Trial `cv × K` Base-Models fitten (K Gruppen je Fold, `base_learner.py`), dass XLearner zusätzlich eine einmalige Pseudo-Outcome-Nuisance (`cv_splits×2`+2) vorberechnet, und dass RCT-Propensity-Tasks auf 20 Trials gedeckelt sind. (2) BLT tunt nicht nur Nuisance-Modelle (Outcome/Propensity der DML/DR-Verfahren), sondern auch die Base-Models der Meta-Learner (SLearner/TLearner/XLearner), die direkt den CATE bilden. Neu: Die Fits werden pro Task aus dem echten `tuning_plan` summiert (`_blt_fits_per_task`, exakt gespiegelt vom Tuning-Code), die Task-Sharing-Tabelle zeigt eine `Berechnung`- und `Fits`-Spalte (Headline = Summe der Zeilen, intern verifiziert), und die Beschreibungen (Intro, Karten-Label „BLT-Fits (alle Tasks)", Fit-Budget-Box, Task-Sharing-Text) nennen korrekt Nuisance **und** Meta-Learner-Base-Models. Der Trial-CV nutzt weiterhin korrekt `tuning.cv_splits` (= echter Code; in UI-Configs identisch zu `dml_crossfit_folds`). Reine Report-/Anzeigekorrektur — kein Einfluss auf Training/Tuning. Golden-Snapshots (`report_golden.html`, `report_golden_external.html`) regeneriert; Änderungen auf die Tuning-Sektion beschränkt (verifiziert). Suite: 192 passed.
- **`uplift_at_k` (uplift@10/20/50%) stimmt jetzt exakt mit dem Uplift-by-Percentile-Plot und der Qini-Kurve überein.** Der Top-k%-Schnitt in `uplift_at_k` (`uplift_metrics.py`) verwendete `ceil(k·n) − 1`, während `_native_uplift_by_percentile` seine Bins über `np.linspace(0, n, n_bins+1).astype(int)` bildet (Floor: `int(n/n_bins)`) — wie auch sklift `uplift_at_k(strategy='overall')` mit `n_size = int(n·k)`. Für Eval-Set-Größen, die kein Vielfaches von 10 sind (der Normalfall), deckte `uplift_at_k` dadurch ein Sample mehr ab als der erste Perzentil-Balken → `uplift@10%` wich vom ersten Balken des Plots ab (im Test ≈ 1,5–2,8e-3) und der „passende" Qini-Kurven-Punkt lag am falschen Schnitt. Jetzt nutzt `uplift_at_k` ebenfalls `n_size = int(k·n)` (Floor). Damit gilt über beliebige `n` exakt: `uplift@10%` = erster Perzentil-Balken, `uplift@20/50%` = kumulative Dezil-Kanten, und der Qini-Kurven-Punkt erfüllt `Q(k) = N_treat(k)·(rate_t − rate_c) = N_treat(k)·uplift@k` — die drei Darstellungen sind voneinander ableitbar. Verifiziert per Internetrecherche gegen die sklift-Quellkonvention (`int(n·k)`, identische Qini-Formel `Y_t − Y_c·N_t/N_c`); kein Einfluss auf die Champion-Selektion (Default-Metrik `qini`-Koeffizient). Betrifft alle `uplift_at_k`-Aufrufer (BT-Eval, historischer Score, MT-`mt_eval_summary`). Neue Regressionstests `TestPlotMetricConsistency` (7, parametrisiert über teilbare/nicht-teilbare `n`). Suite: 192 passed.
- **„App zurücksetzen" beendet jetzt die gesamte Prozessgruppe (statt nur den Eltern-Prozess).** Der `/api/reset`-Endpunkt (`app/routes/analysis.py`) terminierte den laufenden Analyse-Subprozess via `os.kill(pid, SIGTERM)` — also nur den Orchestrator (`run_analysis.py`). Der Lauf wird aber mit `start_new_session=True` in einer eigenen Prozessgruppe gestartet, und die eigentliche CPU-Last steckt in Kindprozessen (LightGBM `n_jobs=-1`, joblib/loky-Worker). `run_analysis.py` hat keinen SIGTERM-Handler, der diese aufräumt → beim Zurücksetzen mitten in einem Lauf starb der Eltern-Prozess, während die Worker-Kindprozesse als Waisen (Reparenting auf PID 1) mit voller CPU-Last weiterliefen. Jetzt nutzt `/api/reset` `os.killpg(os.getpgid(pid), SIGTERM)` mit Fallback auf `os.kill` — konsistent zu `/api/restart-process` (`health.py`) und dem Worker-Timeout-Pfad, die die Gruppe bereits korrekt beenden. Laufzeit-Gegenbeweis bestätigte die alte Lücke (Eltern tot, Kind lebt weiter); neuer Integrationstest `tests/test_reset_endpoint.py` (2 Stück) spawnt eine echte Prozessgruppe und prüft via Flask-Test-Client, dass nach dem POST sowohl Eltern- als auch Kindprozess beendet sind. Suite: 185 passed.
- **Surrogate-Baum-Tiefe für LGBM war im Logging immer `None`.** `_log_surrogate_tree_info` (`analysis_pipeline.py`) las die Tiefe via `tree_info[0].get("max_depth")` aus `booster_.dump_model()`. LGBMs `dump_model()` enthält aber gar keinen `max_depth`-Key (weder pro Baum noch top-level; vorhandene Keys: `num_cat, num_leaves, shrinkage, tree_index, tree_structure`) → die Tiefe war für LGBM-Surrogates konstant `None`, obwohl `num_leaves` korrekt extrahiert wurde. Jetzt wird die Tiefe aus der `tree_structure` abgeleitet (Anzahl Split-Ebenen bis zum tiefsten Blatt) — analog zur korrekten CatBoost-Tiefe (`get_all_params().depth`). Reines Logging/Observability (keine Modell- oder Selektionswirkung). Gefunden via Laufzeit-Verifikation; neuer Test `tests/test_surrogate_tree_info.py` (5 Stück: LGBM-Tiefe ≠ None, CatBoost depth/leaves, None-/Unknown-/Exception-Robustheit als 2-Tupel). Suite: 183 passed. In `_check_and_balance_treatments` (`data_prep_pipeline.py`) wurde der Verlust-Deckel via `float(getattr(dp, "balance_max_loss_frac", 1.0) or 1.0)` aufgelöst. Das `or 1.0` war als None-Schutz gedacht, fing aber auch den legitimen Wert `0.0` ab (in Python ist `0.0` falsy → `0.0 or 1.0 == 1.0`), sodass der strengste Deckel („kein Zeilenverlust erlaubt") stillschweigend zu `1.0` (Deckel aus) wurde — Werte nahe 0 wirkten, exakt `0.0` nicht. Jetzt wird `None` explizit behandelt und ein gesetztes `0.0` erhalten. Die übrigen drei Guardrails (`balance_min_arm_abs`, `balance_min_arm_frac`, `balance_target_grid_step`) sind nicht betroffen, da dort der „Aus"-Wert mit dem Default identisch ist (`0`/`0.0`). Gefunden via Laufzeit-Edge-Case-Batterie; zwei Regressionstests ergänzt (`test_max_loss_frac_zero_is_respected`: Deckel 0.0 feuert die Infeasibilitäts-Warnung; `test_max_loss_frac_caps_candidates`: Deckel 0.12 disqualifiziert das eff-N-Optimum 50% und erzwingt 67%). Suite: 178 passed. Die Session-Fixture `tuning_mod` mockte `rubin.evaluation.uplift_metrics` nicht, das `final_model.py`/`causal_forest.py` inzwischen importieren — dadurch scheiterte das direkte Laden des Tuning-Subpakets und (reihenfolgeabhängig) errorten bis zu 40 Tests, weshalb die Datei aus der regulären Suite ausgeklammert war. Jetzt wird das leichtgewichtige `uplift_metrics` real geladen (statt gemockt) und `rubin.evaluation` als Parent-Package gemockt. Die Datei läuft vollständig (56 passed, 3 skipped) und ist wieder Teil der Suite (gesamt 168 passed).
- **BLT-Fit-Schätzung in der UI: Zeilen-Summe stimmt jetzt mit der Headline überein.** In der `TuningPlanPreview` (`10_page_models.jsx`) war die Headline „Base-Learner-Fits" als `Σ(Trials × Fits pro Task) + pseudoNuisanceOnce` definiert, wobei `pseudoNuisanceOnce = #Pseudo-Effekt-Tasks × (innerCv×2 + 2)` (die einmalig vor den Trials berechnete XLearner-Pseudo-Outcome-Nuisance μ₀/μ₁) nur zum Gesamttotal addiert wurde. Die Per-Task-Tabelle zeigte in der „Fits"-Spalte aber nur `Trials × Fits` (ohne den einmaligen Term), sodass die Spaltensumme um genau `innerCv×2+2` (z. B. 12 bei cv=5) unter der Headline lag — der „krumme" Rest tauchte in keiner Zeile auf, nur im Berechnungs-Text der Pseudo-Zeile und in der Fußnote. Korrigiert: die einmalige Pseudo-Nuisance wird jetzt in die „Fits"-Zahl der jeweiligen Pseudo-Effekt-Zeile eingerechnet (`rowFitsFor`), womit die Spaltensumme exakt der Headline entspricht (der Berechnungs-Text „… cate + N nuis (einmalig)" der Zeile kündigte die Zahl ohnehin schon an). Headline-Wert unverändert. Bundle via `build_ui.py` neu gebaut. Reiner UI-Anzeigewert — kein Produktivcode, kein Einfluss auf das tatsächliche Tuning. (Die gröbere Report-Schätzung `n_studies × n_trials × cv` in `html_report.py` ist davon nicht betroffen — sie modelliert die Pseudo-Nuisance gar nicht und hat keine Per-Task-Aufzählung.)
- **Zwei veraltete Tests in `test_tuning.py` angeglichen** (kein Produktivcode betroffen). (1) `test_fmt_overfit_penalty_scale_floor` erwartete noch den alten Penalty-Scale-Floor `0.1`; der Code nutzt seit der bewussten Umstellung auf Skaleninvarianz `1e-8` (nur Division-by-Zero-Schutz, Gap wird relativ zu `|val_score|` gemessen, damit die Penalty auch auf Qini-Skala greift). Test auf das beabsichtigte `1e-8`-Verhalten umgeschrieben (val=0.005/Gap=100% → 0.00025) plus Nullwert-Sicherheit. (2) `test_both_without_learner_type_fallback` patchte `_build_catboost_classifier` am `rubin.tuning`-Package-Re-Export, während `build_base_learner` (definiert in `rubin.tuning.common`) den Namen in DESSEN Modul-Namespace auflöst — ein Artefakt des früheren Modul-Splits. Patch-Ziel auf `rubin.tuning.common` korrigiert. Voller Suite-Lauf jetzt grün (156 passed).
- **Optuna-Deprecation `consider_endpoints` entfernt.** Der TPESampler wurde mit `consider_endpoints=True` konstruiert; dieser Parameter ist seit Optuna 4.9.0 deprecated (Entfernung für 6.0.0 geplant) und löste bei jedem Study-Aufbau eine `FutureWarning` aus. An allen sechs Sampler-Stellen (BaseLearnerTuner, FinalModelTuner je 2 Fallback-Pfade, CausalForestTuner Qini-/RScore-Pfad) entfernt; Optuna nutzt jetzt den Standard (`consider_endpoints=False`). Wäre der Parameter bis 6.0.0 stehen geblieben, hätte der `try/except`-Fallback ihn dort als `TypeError` abgefangen und auf einen deutlich schwächeren Sampler (ohne `multivariate`/`group`/`constant_liar`) zurückgegriffen. **Hinweis:** Dies ändert die TPE-Sampling-Sequenz minimal (Endpunkt-Heuristik der Parzen-Varianz) und damit die explorierten Hyperparameter — eine Exploration-/Konventionsänderung, keine Korrektur. Docstrings/Kommentare in `base_learner.py` und `common.py` entsprechend angepasst. (Die separaten `ExperimentalWarning`s zu `multivariate`/`group`/`constant_liar` bleiben — sie sind bewusst genutzte, stabile Features, keine Deprecation.)
- **UI-Fit-Schätzung an XLearner-Pseudo-Caching angepasst.** Nachdem die XLearner-Pseudo-Outcomes nur noch einmalig (statt pro Trial) berechnet werden, zählten die Fit-Schätzer die Pseudo-Outcome-Nuisance (`innerCv×2 + bis zu 2 Fallback`) weiterhin pro Trial. Korrigiert in der App-Seitenleiste (`14_app.jsx`: `tr × (tc×K) + (innerCv×2+2)` statt `tr × ((tc×K) + (innerCv×2+2))`) und in der Tuning-Plan-Vorschau (`10_page_models.jsx`: Pseudo-Effekt-Task zählt pro Trial nur noch `nCv×K` CATE-Fits, die Nuisance einmalig als `pseudoNuisanceOnce`; Label und Erklärtext angepasst). Beispiel (50 Trials, cv=5): Pseudo-Task-Schätzung 1100 → 512 Fits, beide Schätzer konsistent. Bundle via `build_ui.py` neu erzeugt. Die grobe Report-Schätzung (`n_studies × n_trials × cv`) ist davon unberührt, da sie den Pseudo-Nuisance-Term ohnehin nie pro Trial enthielt.
- **Überflüssiges MLflow-Artefakt `uplift_curve__<historical_score>.csv` entfernt.** Die rohe Uplift-Kurven-CSV des historischen Scores wurde geloggt, aber nirgends zurückgelesen (kein Consumer), ihre Kennzahlen (`qini`, `auuc`, `uplift10/20/50`, `policy_value`) waren bereits als MLflow-Metriken vorhanden und die Kurve in den Vergleichs-Plots visualisiert. Zudem war sie asymmetrisch — die eigentlichen kausalen Modelle erhielten keine analoge Roh-CSV (nur PNG-Plots + Metriken). Die komplette Auswertung des historischen Scores bleibt unverändert; nur der ungelesene Roh-Dump entfällt.
- **KRITISCH: CausalForestDML verlor im RCT-Modus die konstante Propensity nach CFT-Tuning.** Der CFT-Rebuild konstruierte einen frischen `ModelContext` (`_ctx_new`) **ohne** `is_rct` (Default `False`) — und ohne den `forest_fixed_params`-Merge und `parallel_jobs=-1`. Folge: Lief ein CausalForestDML im RCT-Modus durch das CausalForest-Tuning (`use_econml_tune=true`), wurde das ausgelieferte Modell mit **geschätzter** statt konstanter Propensity (`DummyClassifier`) gebaut — obwohl das Tuning selbst mit konstanter Propensity lief (Tune/Train-Mismatch) und der Log „RCT-Modus → konstante Propensity" das Gegenteil behauptete. Fix: Der CFT-Rebuild verwendet den bereits korrekt gebauten `ctx` wieder (enthält `is_rct`, forest_fixed_params, `parallel_jobs=-1`) und überlagert nur die getunten Forest-Params. Verifiziert: `model_t` ist im RCT-Modus ein `DummyClassifier` und bleibt es nach dem CFT-Overlay (mit übernommenen getunten Forest-Params); im Beobachtungs-Modus bleibt es der geschätzte Base-Learner. Ohne CFT war der Pfad bereits korrekt (erster `ctx`-Bau mit `is_rct`).
- **Log-Konsistenz: `model_final`-Parameter jetzt auch für CausalForestDML/CausalForest.** Der „model_final effektive Params"-Log existierte nur für NonParamDML/DRLearner. Für die Forest-Modelle (deren „model_final" der Causal Forest selbst ist) wird nun nach dem CFT-Block analog ein „model_final (Forest) effektive Params"-Log ausgegeben (forest_fixed_params + CFT-getunte Params, `cft_tuned=ja/nein` als Pendant zu `explicit_tuned`). Damit loggt jedes Modell mit tunebarer Final-Stufe seine effektiven Parameter.
- **Feature-Selektion liefert jetzt exakt `max_features` (Verhaltensänderung).** `select_features_by_importance` kappte die Union der Pro-Methode-Top-N nur nach oben auf `max_features`, füllte aber nie auf. Bei Überschneidungen zwischen Methoden (dieselben starken Features bei CatBoost *und* CausalForest) war die Union kleiner als das Budget → es kamen weniger Features heraus als eingestellt (z.B. 48 statt 77). Fix: Das Ergebnis wird auf `target = min(max_features, n_pool)` gebracht — bei Überschreitung wie bisher per Konsens-Rang gekappt (Verhalten **byte-identisch** zum Vorzustand, verifiziert), bei Unterschreitung neu mit den global bestbewerteten verbleibenden Features aufgefüllt. Die Pro-Methode-Top-N bleiben als garantierte Repräsentation erhalten. `max_features` ist damit eine **exakte Zielzahl** statt einer Obergrenze (Ausnahme: weniger Features im Pool nach dem Korrelationsfilter → alle verbleibenden). Propagiert nach UI (Label „Ziel-Feature-Anzahl", Hinweis-/Vorschautexte), HTML-Report, `rubin_overview.html` und Docs (`konfiguration.md`, `architektur.md`, `README_DE.md`). Neue Tests sichern die Exaktheit (Overlap-Auffüllung, Kappen, Pool < Budget, 1–3 Methoden).
- **BLT-Performance: XLearner-Pseudo-Outcomes nur noch einmal berechnet.** `_build_xlearner_pseudo_outcomes` (2K OOF-Nuisance-Fits) wurde bisher *innerhalb* des per-Trial-Objectives (`_objective_xlearner_cate`) aufgerufen, obwohl die Pseudo-Outcomes nur von den (über alle Trials fixen) getunten Outcome-Nuisances + `tuning_cv_seed` abhängen, nicht von den Trial-Params der `cate_models`. Sie wurden so `n_trials`× redundant neu gebaut. Fix: Vorberechnung einmalig in `_tune_task` vor `study.optimize`, Übergabe als `pseudo`-Array an das Objective. Ergebnis byte-identisch (Pseudo-Outcomes sind deterministisch — verifiziert: zwei Aufrufe identisch, XLearner-Tune über zwei Läufe reproduzierbar), nur ohne die redundanten Fits.
- **Skill-Score multiclass-fähig (Diagnose).** `_compute_skill_score` nutzte als Klassifikations-Baseline die *binäre* Entropie von `y.mean()` und war damit bei MT-Propensity (K>2 Arme) degeneriert (Baseline ≈ 0 → Skill 0). Fix: Baseline = Entropie der Klassenverteilung `−Σ p_k·ln(p_k)` (= Log-Loss des konstanten Klassen-Prior-Prädiktors), multiclass-fähig. Für binäres Y exakt identisch zur alten Formel (verifiziert byte-genau). Betrifft nur die Diagnose-/Report-Anzeige, nicht Selektion oder Hyperparameter-Wahl.
- **Toter Parameter entfernt:** Der ungenutzte `multiclass`-Parameter von `BaseLearnerTuner._score_classifier` (die Multiclass-Behandlung erfolgt korrekt über `proba.ndim`) samt seiner vier Aufrufstellen entfernt.
- **FMT-Refactor: NonParamDML/DRLearner-Duplikation entfernt.** Die ~150 Zeilen Scorer-Wahl + Qini-/RScore-Objective + `study.optimize` + Result-Extraktion waren in beiden Modell-Zweigen nahezu identisch dupliziert. Extrahiert in eine gemeinsame Methode `_run_fmt_study(...)`; die Zweige bauen nur noch ihre estimator-spezifischen `cached_folds` und reichen die (rollennamen-aufgelösten) Nuisance-Params herein (`final_model.py` 842 → 556 Zeilen). Verhaltenserhaltend verifiziert: FMT-`model_final`-Auswahl für NonParamDML ist byte-identisch zum Vorzustand, für `scorer=rscore` UND `scorer=qini`.
- **FMT-RScorer-Cache: Nuisance-Fingerprint im Cache-Key.** Der Scorer-Cache-Key (`_outer_splits, tuning_cv_seed, single_fold, len(Y), op>0`) enthielt nicht die Nuisance-Konfiguration. Wurden NonParamDML und DRLearner beide FMT-getunt, verwendete das zweite Modell die Scorer des ersten — gebaut mit dessen Nuisance-Hyperparametern (reihenfolge-abhängig). Fix: deterministischer Fingerprint von (Outcome-, Propensity-, Base-fixed-Params) + `base_type` + `is_rct` im Key. Identische Configs teilen sich weiterhin den Cache (Speed bleibt), unterschiedliche bekommen jeweils einen eigenen, mit den eigenen Nuisance-Params gebauten Scorer.
- **FMT-Docstring korrigiert:** `_apply_overfit_penalty`-Docstring nannte fälschlich `scale = max(|val_score|, 0.1)`, der Code nutzt (skaleninvariant) `1e-8`. Docstring an den Code angeglichen.
- **CFT-RScorer-Cache: Schlüssel hinzugefügt.** `CausalForestTuner._scorer_cache` wurde bei jedem zweiten `tune()`-Aufruf (CausalForest nach CausalForestDML) blind wiederverwendet, ohne zu prüfen, ob Nuisance-Params, `base_type`, RCT-Status und Fold-Setup übereinstimmen. In der bestehenden Pipeline ist die Wiederverwendung sicher (der Caller liefert für beide Modelle identische BLT-Nuisances + Splits), aber unbemerkte Abweichungen hätten still falsche Scorer reused. Fix: `_scorer_cache_key` aus Nuisance-Fingerprint + `base_type` + `is_rct` + `inner_cv` + `tuning_cv_seed` + `single_fold` + `len(Y)`; Reuse nur bei Übereinstimmung, sonst Neuaufbau (verifiziert: identische Nuisance → Reuse, abweichende → Rebuild).
- **CFT-Sampler-Seed pro Study (Verhaltensänderung).** Beide Optuna-Sampler (Qini- und RScore-Pfad) nutzten direkt `seed=tuning_cv_seed`, wodurch die CausalForestDML- und CausalForest-Studies bei identischem Suchraum exakt dieselbe Trial-Sequenz durchliefen. Umgestellt auf einen pro-Study aus `tuning_cv_seed + sha256(study_key)` abgeleiteten Seed (`_study_seed`, analog zu BaseLearnerTuner/FinalModelTuner) — CFDML und CF explorieren jetzt unterschiedliche Sequenzen (verifiziert: verschiedene Seeds, weiterhin reproduzierbar). **Hinweis:** Dies ändert bewusst die explorierte Sampler-Sequenz und damit die getunten Forest-Parameter — es ist eine Exploration-/Konventionsänderung, keine Output-Korrektur. Der Fold-Split-Seed (Stratifizierung) bleibt unverändert `tuning_cv_seed`.
- **Fold-Daten-Cache: automatische Eviction (Memory-Leak-Härtung).** Der Modul-Cache `_fold_data_cache` in `training.py` (Fallback für `DataFrame.attrs`, falls diese durch eine DataFrame-Operation verloren gehen) war mit `id(df)` gekeyt und wurde nie geleert. Über viele Analysen hinweg wuchs er unbegrenzt; zudem konnte CPython eine `id()` nach GC des ursprünglichen DataFrames für ein neues Objekt wiederverwenden, sodass ein stale Eintrag fälschlich getroffen würde. Fix: In `store_fold_aligned_data` wird ein `weakref.finalize(df, _fold_data_cache.pop, id(df), None)` registriert → der Eintrag wird entfernt, sobald das DataFrame garbage-collected wird. Der Finalizer hält (nur `id(df)` als int + die Modul-Methode) keine starke Referenz auf das DataFrame und verhindert dessen GC daher nicht. Verifiziert: Round-Trip + attrs-Fallback unverändert, Eintrag verschwindet nach df-GC, 50 Wegwerf-DataFrames hinterlassen 0 Cache-Einträge.
- **CFT-Tuning ignorierte `mc_iters`/`mc_agg`:** Der cache_values-`CausalForestDML` im CausalForest-Tuning (`tuning/causal_forest.py`) wurde ohne `mc_iters`/`mc_agg` gebaut → tunte die Forest-Parameter auf mc_iters=1-Nuisance-Residuen, während FMT, der zentrale Model-Builder und das finale/Produktions-Training `mc_iters=N` nutzen. Bei `mc_iters>1` wurden die Forest-Hyperparameter also unter anderen (höhervarianten) Residuen optimiert als das ausgelieferte Modell sieht. Fix: `mc_iters`/`mc_agg` aus der Config gesetzt (analog FMT), `_CFDML_EXPLICIT_KEYS` um beide ergänzt (Schutz gegen Doppel-Argumente). Verifiziert: CFDML mit `mc_iters>1` + cache_values + setattr + `refit_final()` reproduziert byte-genau einen Fresh-Fit. Default `mc_iters=None` (=1) bleibt verhaltensgleich → kein Regressionsrisiko; macht zugleich die Fit-Budget-Schätzungen (die `mc` bereits einrechneten) für CFT korrekt.
- **App-Seitenleiste: Modell-Fit-Schätzung nach DRTester-Fix nachgezogen.** `estimateFits()` (14_app.jsx) rechnete für die DRTester-Nuisance `isExternal ? 10 : 20` — die „20" im Cross/TMES-Modus zählte die `fit_nuisance`-Berechnung auf Train UND Val. Da der `dr_train_`-Pfad entfernt wurde (nur noch Val), ist es jetzt in beiden Modi val-only (`f += 10`). Vorher wurde der Cross-Modus um 10 Fits überschätzt. Bundle via `build_ui.py` neu erzeugt.
- **App-UI: FMT-Vorschau zeigt „Trials / Study"-Karte (Konsistenz mit CFT).** Die CausalForest-Tuning-Vorschau zeigte drei Karten (Optuna-Studies, Modell-Fits, Trials/Study), die Final-Model-Tuning-Vorschau nur zwei — die Trials standen dort nur in der Berechnungs-Detailspalte. Reine Anzeige-Inkonsistenz, kein funktionaler Unterschied; FMT zeigt die Karte jetzt ebenfalls.
- **KRITISCH: Bundle enkodierte Rohdaten in Production falsch:** Der Bundle-Preprocessor war ein `SimpleCSVPreprocessor` (nur Schema/Dtype) und trug **nicht** die gelernten kategorialen Encoding-Maps (string→int) und numerischen NA-Fill-Werte des DataPrep-`FittedPreprocessor`. Bekommt Production Rohdaten, enkodierte `.astype("category")` Kategoriale mit batch-abhängigen Codes (≠ Training) und ließ numerische NaN ungefüllt → falsche CATEs. Fix: Der Bundle-Export lädt den DataPrep-`FittedPreprocessor` (liegt neben `x_file`), schränkt ihn via `restrict_fitted_preprocessor` auf die selektierten Features ein und bündelt diesen → Production enkodiert Rohdaten korrekt (Maps + NA-Fill) und reduziert in einem Schritt auf die Modell-Features. Verifiziert: Production reproduziert aus Rohdaten die exakte Analyse-Feature-Matrix; unbekannte Kategorien → -1. Fallback (mit Warnung) auf Schema-only, wenn der DataPrep-Preprocessor fehlt oder selektierte Features ihm unbekannt sind.
- **KRITISCH: Champion-Auswahl konnte NaN-Modell wählen:** `_safe_float` in `model_management.py` gab für `NaN`/`Inf` den Wert selbst zurück (da `float(nan)` gelingt) statt `None`. Ein Modell mit nicht-endlicher Selektionsmetrik (z. B. NaN-Qini bei leeren Quantil-Bins / geringer CATE-Varianz) wurde dadurch nicht ausgeschlossen und konnte wegen der undefinierten NaN-Vergleiche im Sort fälschlich als Champion gewählt und ins Bundle/in Production befördert werden. Fix: `_safe_float` schließt nicht-endliche Werte aus (`math.isfinite`). Normale Auswahl/Tiebreak unverändert.
- **DRTester: toter/latenter Train-Nuisance-Pfad entfernt:** `CustomDRTester.fit_nuisance` berechnete `dr_train_` via `fit_nuisance_cv(Xtrain)`, das von keiner `evaluate_*`-Methode gelesen wird (EconML nutzt `cate_preds_train_`, nicht `dr_train_`). Im Cross-/TMES-Modus war das eine redundante Nuisance-Cross-Fitting-Berechnung auf denselben Daten; zudem latenter Crash, falls die Funktion je mit unterschiedlich langen Train/Val aufgerufen wird (der vorberechnete T×Y-Splitter ist auf Val-Längen-Strata gebaut). Fix: nur noch `dr_val_` berechnen (`dr_train_ = None`). Keine Ergebnisänderung.
- **App Path-Traversal-Härtung:** `/api/download`, `/api/view` und der Upload-`target_dir`-Check nutzten `str(full).startswith(str(ROOT.resolve()))` — ein String-Prefix ohne Separator, der ein Geschwister-Verzeichnis mit gleichem Namens-Präfix (z. B. `<root>_backup`) fälschlich zuließ. Fix: `Path.is_relative_to(...)`. Zusätzlich Upload-Response gegen `relative_to(ROOT)`-`ValueError` abgesichert, falls das Ziel unter `WORK_DIR` (statt `ROOT`) liegt.

- **Uplift@k-Metrik (BT + MT):** `uplift_at_k` lieferte den fraction-skalierten Uplift-Kurvenwert `(rate_t − rate_c)·k` statt der Within-Group-Inkrement-Rate `rate_t − rate_c`. Dadurch waren `uplift_at_10/20/50pct` (und `uplift{10,20,50}_T{arm}`) um den Faktor k zu klein und stimmten nicht mit dem Uplift-by-Percentile-Plot / sklift überein. Fix: unskalierte kumulative Rate aus den Kurven-Counts (sklift `uplift_at_k(strategy='overall')`).
- **Qini-/AUUC-Metrik konsistent mit Plot + sklift:** `qini_coefficient` wurde aus der Uplift-Kurve berechnet, während `_native_qini_curve` die echte Qini-Kurve `Y_t − Y_c·N_t/N_c` zeichnet → der annotierte AUC passte nicht zur gezeichneten Kurve. `qini_coefficient` nutzt jetzt die Qini-Kurve (sklift `qini_auc_score`, pro Population normiert); `auuc` ist jetzt die Fläche über der Random-Baseline auf der Uplift-Kurve (sklift `uplift_auc_score`). Beide Änderungen sind rangerhaltend — die Champion-Auswahl bleibt unverändert (empirisch: Pearson ≥ 0.9998, gleicher argmax).
- **ACHTUNG — Skalenbruch:** Die obigen Metrik-Korrekturen ändern die Absolutwerte von `uplift_at_*`, `qini` und `auuc` in Report und MLflow. Historische MLflow-Werte sind danach nicht mehr maßstabsgleich mit neuen Läufen (Modell-Ranking/Champion-Auswahl jedoch unverändert).
- **`test_high_threshold_removes_nothing` spröde:** Die synthetische Korrelation des Fixtures (0.99994) lag über dem Test-Threshold 0.999 → 'b' wurde korrekt entfernt, der Test erwartete aber fälschlich „nichts entfernt". Fix: Fixture-Noise angehoben auf corr(a,b) ≈ 0.96 (klar zwischen den getesteten Schwellen 0.9 und 0.999).
- **KRITISCH: `_log_loss_fn` fehlender Import** in `base_learner.py` nach Modul-Split. `_score_classifier()` crashte mit NameError, still gefangen durch `except Exception: return 0.0` → alle Trials scored 0.0 → `best=0, skill=1.0000`. Fix: `from sklearn.metrics import log_loss as _log_loss_fn` in `base_learner.py`.
- **`current_metric` NameError** in RCT-Warnung: Variable war nicht definiert, `except Exception: pass` verschluckte den Fehler. Fix: Hardcoded `neg_log_loss=%.4f` Format-String.
- **`analysis_pipeline.py` Strukturbruch:** Dtype-Exception-Handler-Edit löschte versehentlich den äußeren `except FileNotFoundError:`-Block. Fix: Struktur wiederhergestellt.
- **`final_model.py` fehlender `import gc`:** Module-Level-Import fehlte nach Extraktion (nur lokale `import gc as _gc` vorhanden). Fix: Module-Level-Import + lokale Aliase bereinigt.
- **`top_pct` in 3 YAML-Configs:** `config_binary_treatment.yml`, `config_explainability.yml`, `config_reference_all_options.yml` enthielten das gelöschte Feld → `extra=forbid` ValidationError. Fix: Zeilen entfernt.
- **`scikit-uplift` in `app/requirements_app.txt`:** Phantom-Dependency, war bereits aus `pyproject.toml` entfernt. Fix: Auch aus App-Requirements entfernt.
- **`test_feature_selection.py` komplett veraltet:** Importierte `_top_pct_features` (gelöscht) und rief `select_features_by_importance(top_pct=...)` auf (Parameter entfernt). Fix: Tests auf `max_features`-API umgeschrieben.
- **`test_settings.py` veraltete Assertions:** `max_features is None` (Default ist 77), `methods == ["lgbm_importance"]` (Default ist `["catboost_importance"]`), `top_pct`-Assertions. Fix: Gegen aktuelles Schema korrigiert.
- **`config_explainability.yml`:** SHAP + Surrogate waren beide deaktiviert. Fix: `calculate_shap_values: true` + `surrogate_tree.enabled: true`.
- **`config_speed.yml`:** 50 BLT-Trials bei Speed-Preset. Fix: Auf 20 reduziert.
- **Doku-Fixes:** Forest-Defaults-Tabelle (min_samples_leaf/split), Overfit-Penalty-Formel (0.1 Floor), `tune_causal_forest()` → `CausalForestTuner.tune()`, Grid-Search → Optuna TPE, FMT-Parallelisierung (cache_values), `method`-Phantom-Feld in explainability.md, `rubin/utils/uplift_metrics.py` → `rubin/evaluation/`.

- **Externer RScorer für FMT + CFT:** Scoring über EconML `RScorer` mit unabhängiger Nuisance (eigene model_y/model_t, 2-Fold T×Y-stratifiziertes Cross-Fitting auf Val-Daten). Ersetzt `est.score() + DummyRegressor`-Normalisierung. Vorteile: kein Overfitting-Leak, einheitliche R-Score-Metrik über alle Learner (NonParamDML, DRLearner, CausalForestDML, CausalForest via Adapter), Konsistenz mit EconML `tune()`. Referenz: Schuler et al. (2018), Nie & Wager (2021).
- **n_estimators-Trennung (Tuning vs. Production):** Tuning verwendet immer `n_estimators=100` (EconML tune()-Konvention, 5× Speedup). Production-Modelle verwenden `n_estimators=500` (Default, via `forest_fixed_params` konfigurierbar). `_CFT_TUNING_N_ESTIMATORS` Konstante in `tuning_optuna.py`.
- **CausalForestAdapter im GRF-Scoring:** Der reine CausalForest wird für RScorer-Kompatibilität über den existierenden `CausalForestAdapter` (erbt von `BaseCateEstimator`) gewrappt. `scorer.score(adapter)` statt manueller MSE-Berechnung.
- **RCT Konstante Propensity:** Bei `study_type: "rct"` wird für alle Propensity-Rollen (`model_t`, `model_propensity`, `propensity_model`) ein `DummyClassifier(strategy="prior")` eingesetzt. BLT diagnostiziert vorher mit 20 Trials, ob Propensity-Skill ≈ 0 (bestätigt RCT). Training, FMT-Caching und CFT-Caching nutzen konsistent die konstante Propensity.
- **Optuna TPE-Verbesserungen:** `consider_endpoints=True` (Randexploration), `n_warmup_steps=2` (stabileres Pruning), `n_startup_trials` ≤ 50% der Trials (TPE aktiviert sich bei Propensity-Tasks). Konsistent über BLT, FMT, CFT.
- **Lineare Suchraum-Exploration:** `log=True` von allen Parametern entfernt außer `learning_rate`. Gleichmäßige Exploration statt logarithmischer Konzentration am unteren Rand.
- **Speicher-Optimierungen (BLT):** `del model` in allen 5 Objectives nach jedem Fold, `malloc_trim(0)` zwischen Tasks — glibc gibt C++-Speicher ans OS zurück.
- **SHAP-Farbpalette:** Custom SHAP-Plots (CATE-Profile, SHAP-Dependence) nutzen jetzt SHAP-Standardfarben (`#ff0051`/`#008bfb`) statt rubinrot.

- **CausalForestDML `set_params` existiert nicht:** CausalForestDML ist kein sklearn `BaseEstimator` → `set_params()` wirft `AttributeError`, still gefangen durch `except Exception`. Alle 100 CFT-Trials schlugen sofort fehl (Score -1e12 in 1 Sekunde). Fix: `setattr(est, k, v)` statt `est.set_params(**params)`. Empirisch verifiziert: `setattr` + `refit_final()` produziert identische Ergebnisse wie Fresh-Fit.
- **CFT Suchraum-Reduktion (9 → 4 Parameter):** Aligniert mit EconML `tune()`. Nur noch `max_depth` (kategorisch), `min_weight_fraction_leaf` (log), `min_var_fraction_leaf` (log), `criterion` (mse/het). Alle anderen Forest-Parameter auf EconML-Defaults fixiert (`n_estimators=100`, `min_samples_leaf=5`, `max_samples=0.45`). Verhindert Intercept-Kollaps und verbessert TPE-Effizienz.
- **`_fmt_is_rct` UnboundLocalError:** Variable war im NonParamDML-Block definiert, DRLearner-Block konnte sie nicht sehen (separate `tune_final_model`-Aufrufe pro Modell). Fix: Definition vor die if/elif-Branches verschoben.
- **CausalForestDML `max_samples`:** Fixiert auf EconML-Default 0.45 (nicht mehr im CFT-Suchraum). Bei `inference=True` maximal 0.5 erlaubt.
- **Pipeline Progress-Tracking:** `_progress("Base-Learner-Tuning")` nur noch bei aktivem BLT. `total`-Schrittzahl schließt BLT nur ein wenn aktiviert. Behebt ausgegraute BLT/FMT/CFT-Steps bei deaktiviertem Tuning.
- **BLT "Direkt verwendet" Badge:** Farbgebung von gold (FMT) auf rosé/rubinrot (BLT) korrigiert.
- **DRLearner `discrete_outcome=True`:** Konsistent in BLT (Logloss-Objective), Training und Doku.
- **Surrogate-Leakage beseitigt (Fold-Aligned Predictions):** K-Fold CV nutzt jetzt Fold-Aligned Predictions: für Surrogate-Fold k wird Champion_k's Prediction verwendet (Champion_k hat Fold k nie gesehen → komplett leakage-frei). Strenger als Standard-OOF-Stacking (eliminiert auch indirektes Leakage). Final-Fit für Produktion nutzt weiterhin Full-Data-Refit. Cross-Prediction sammelt jetzt per-Fold Full-Dataset-Predictions in `DataFrame.attrs["fold_aligned_preds"]`.
- **Surrogate volle Evaluation:** DRTester-Plots (BLP, Calibration, Qini/TOC-CIs, Policy Values), Score-Redistribution und Custom Qini vs. Historisch jetzt auch für den Surrogate.

### Entfernt
- **scikit-uplift (sklift):** Komplett entfernt (Import, Dependencies, `_ensure_sklift`, Funktionsnamen). Alle Uplift-Plots nativ implementiert.
- **`lgbm_permutation`:** Feature-Selektionsmethode entfernt.
- `_first_crossfit_train_indices()` (26 Zeilen Dead Code, ersetzt durch Dual-Seed-Schutz).
- `crosspred_splits` Parameter aus `tune_final_model()`.
- 68 Zeilen Dead JSX (Tag, ColumnPicker, ROLES, BT_PRESETS, MT_PRESETS, etc.).
- REVIEW_PLAN.md, REVIEW_OVERVIEW.md, REVIEW_REPORT.md aus docs/.


Alle relevanten Änderungen am rubin-Framework. Neueste Einträge oben.

## 2026-04-22 (Session 11 — CV-Harmonisierung, Tuning-Metrik, Signatur-Trennung)

### Kritische Fixes (Modellqualität)

- **Alle internen CVs auf EconML-Default (2) gesetzt:** BLT `tuning.cv_splits`, FMT `final_model_tuning.cv_splits`, DML `dml_crossfit_folds` — jeweils unabhängig von `cross_validation_splits` (5, äußere Evaluation). Behebt Inkonsistenz, bei der interne CVs versehentlich auf 3–5 standen und die Tuning-/Produktions-Datenmengen nicht zusammenpassten.
- **FMT `cv_splits` als eigenes Config-Feld wiederhergestellt** (Default 2). War entfernt worden, wodurch FMT `cross_validation_splits` (5) nutzte — 2.5× langsamer pro Trial und andere Scoring-Dynamik als Produktion.
- **FMT `stability_penalty` auf 0.3 zurückgesetzt** (war 0.0). Ohne Penalty fand Optuna aggressive model_final-Params, die out-of-sample schlecht generalisierten.
- **Tuning-Metrik von `pr_auc` auf `log_loss` zurückgesetzt.** Log-Loss misst Kalibrierung — essentiell für DML-Residualisierung `Y − E[Y|X]`. PR-AUC optimiert Ranking, was gut kalibrierte Nuisance-Vorhersagen nicht garantiert. Betrifft: settings, tuning_optuna, html_report, UI, config_reference, docs.
- **FMT DRLearner: Äußere Score-Folds = `cross_validation_splits` (5),** interne DRLearner cv = `final_model_tuning.cv_splits` (2). Vorher wurden beide aus demselben Feld gelesen.
- **Pipeline: `_dml_cv` liest jetzt `dml_crossfit_folds`** (Default 2), nicht mehr `cross_validation_splits` (5).

### Task-Sharing Signatur-Trennung

- **Neue `sample_scope`-Werte:** `"all"` (DML/DR-intern, ~40% N in Produktion) vs. `"all_direct"` (Meta-Learner, ~80% N). Verhindert, dass Modelle mit unterschiedlichen effektiven Trainingsmengen dasselbe Tuning teilen.
- **Betroffene Tasks:**
  - `outcome_regression`: DRLearner.model_regression (all) ↔ SLearner.overall_model (all_direct) — jetzt getrennt.
  - `propensity`: DML/DR (all) ↔ XLearner.propensity_model (all_direct) — jetzt getrennt.
- **7 Tasks statt 5** bei vollem Modell-Set (bei per_role/per_learner=false).

### Fit-Berechnungen korrigiert

- **`estimateFits`:** DML-internes cv von `cvSplits` (5) auf `dmlCv` (2) korrigiert → `fitsPerFit` von 11 auf 5 pro DML/DR-Modell.
- **`estimateFits` BLT:** Innere CV von `cvSplits` (5) auf `bltCv` (2) korrigiert → halbiert die BLT-Fit-Schätzung.
- **`estimateFits` BLT Tasks:** Spiegelt jetzt die Signatur-Trennung wider (7 Tasks).
- **`FinalTuningPlanPreview` NonParamDML:** Korrigiert von 1 fit/trial auf `cv×2+1 = 5` fits/trial (RScorer-Setup + full NonParamDML.fit() pro Trial).
- **`FinalTuningPlanPreview` DRLearner:** Korrigiert von `nCv` (2) outer folds auf `outerCv` (5), plus interne Fits pro Fold (5) = 25 fits/trial.
- **CausalForestDML Tune Preview:** Nuisance-Fits von `cvSplits×2` (10) auf `dmlCv×2` (4) korrigiert.

### Sonstige Änderungen

- **`default`-Key aus `tuned_baselearner_params.json` und MLflow-Params gefiltert.** War ein interner Fallback-Lookup-Key, der User-facing keinen Mehrwert hatte.
- **savePreds/predsFormat aus UI entfernt** (Cross-Predictions werden immer automatisch in MLflow geloggt).
- **External Eval UI redesigned:** Kompaktere Card, klarere Input-Labels (X/Features, Y/Outcome, T/Treatment, S/Score), orphaned Closing-Tags entfernt.
- **UI Help-Texte aktualisiert:** cvSplits beschreibt jetzt nur äußere Cross-Predictions; DML-internes cv separat erklärt.
- **YAML-Emission:** `tuning.cv_splits` und `dml_crossfit_folds` werden nicht mehr emittiert (Backend-Defaults greifen). Nur bei Abweichung vom Default.
- **Config Reference:** Alle neuen Defaults dokumentiert (cv_splits=2, dml_crossfit_folds=2, stability_penalty=0.3, metric=log_loss).

## 2026-04-17 (Session 10 — Class Balance, Verzeichnis-Konsolidierung, UI-Polish)

### Evaluiert & entfernt: Class-Balance / Imbalance-Weighting
- **`class_balance`-Feature evaluiert und entfernt:** Tunbares `scale_pos_weight` (Optuna) + `CalibratedClassifierCV` wurden implementiert und getestet. Bei extremer Imbalance (1:1000) war der Effekt auf PR-AUC minimal (0.015 → 0.017), da `scale_pos_weight` primär die Kalibrierung ändert, nicht das Ranking. Die Split-Struktur von Gradient-Boosting-Modellen ist bereits ohne Gewichtung auf die seltene Klasse fokussiert (Log-Loss-Gradienten). Das Feature wurde entfernt, da es ein selbstverursachtes Kalibrierungsproblem erzeugt, das dann per `CalibratedClassifierCV` (3× Trainingszeit) repariert werden muss — ohne Netto-Mehrwert.

### Verzeichnis-Konsolidierung
- **Alle Server-Schreibpfade unter `runs/`:**
  - Feature Dictionary: `ROOT/data/` → `WORK_DIR/exports/`
  - Config Save: `ROOT/configs/` → `WORK_DIR/configs/`
  - Config Import: `ROOT/config_imported.yml` → `WORK_DIR/.rubin_cache/`
  - Edge-Case-Fix: `relative_to(ROOT)` Fallback bei externem WORK_DIR.
- **Bundle-Pfad `runs/bundles` konsistent:** `settings.py` (`BundleConfig.base_dir`), `artifacts.py` (`ArtifactBundler.__init__`), `analysis_pipeline.py` (Fallback), UI (DEFAULT_CFG, YAML-Emission), 16 Config-Templates, Docs, `run_promote.py` Docstring.
- **`.gitignore` bereinigt:** Keine Legacy-Sektion, nur `runs/` + Standard-Ignores. `configs/` (Template-Configs) wird korrekt NICHT ignoriert.

### Repo-Struktur
- **`uplift_metrics.py` verschoben:** `utils/uplift_metrics.py` → `evaluation/uplift_metrics.py`. Kein Backward-Compat-Shim (Grundversion). Imports in `analysis_pipeline.py` + `test_uplift_metrics.py` aktualisiert.
- **Toter Code entfernt:** `grouped_outcome` Classifier-Variante (Methode `_objective_grouped_outcome`, Dispatch, FLAML-Set, UI-Maps, Pipeline-Label-Map) — `_role_signature()` gibt diesen Wert nie zurück.

### UI-Optimierungen
- **Datenpfade-Seite redesigned:** 1 zusammenhängender `<Sec>`-Block statt 2. Uppercase-Sektions-Labels (TRAININGSDATEN, EXTERNAL EVAL, BENCHMARK). Eval-Felder nur bei aktivem Toggle sichtbar. Benchmark nur bei gesetzter S-Datei.
- **Ausgabe vereinfacht:** `outputDir` in Expander „Erweiterte Ausgabe-Optionen" verschoben. Info-Text klarstellt: „Ergebnisse werden automatisch in MLflow geloggt."
- **Bundle-Export vereinfacht:** `bundleDir`-Eingabefeld entfernt, nur Hinweistext `runs/bundles/<timestamp-id>/`.
- **Arbeitsverzeichnis:** Vollständige Ordnerstruktur als Tree-Darstellung. Label: „Eigenes Arbeitsverzeichnis (optional)".
- **FM-Tuning Labels:** `FMT` → `FM-Tuning`, `FMT Intensiv` → `FM-Tuning Intensiv`. User-visible Text konsequent angepasst (FM-Tuning-Trials, FM-Tuning verwendet).
- **Sprachmischung bereinigt:** `Higher is better` → `Höher = besser`, `Stability Penalty` → `Stabilitäts-Penalty`.
- **Placeholder-Pfade:** `data/raw/eval_data.csv` → `runs/data/eval_data.csv`, `data/feature_dictionary.xlsx` → `runs/exports/feature_dictionary.xlsx`.
- **DataPrep Pflicht-Spalten:** Target- und Treatment-Felder starten jetzt leer (nicht vorbefüllt mit Y/T). Placeholders: „z.B. Y, OUTCOME, ..." / „z.B. T, TREATMENT, ...".

### Config-Templates
- `config_reference_all_options.yml`: `class_balance` mit Kommentar hinzugefügt.
- `config_grf_focus.yml`: Copy-Paste-Fix `output_dir: runs/dml_focus` → `runs/grf_focus`.

## 2026-04-16 (Session 9 — UX + Tiefenprüfungen)

### MLflow-Overview-Metriken
- **Champion Top-Level-Logging:** Nach der Champion-Auswahl werden jetzt dedizierte Metriken und Tags geloggt, die in der MLflow-Experiment-Übersicht als Columns sichtbar gemacht werden können (einmalige Auswahl in der UI, wird im Browser-localStorage persistiert):
  - `champion_score` (Metrik): Wert der konfigurierten Selection-Metric für den Champion
  - `champion_qini`, `champion_auuc`, `champion_policy_value` (Metriken): Zusätzliche Performance-Werte für Schnellvergleich
  - `rubin.champion_model` (Tag): Name des Champions (z.B. "NonParamDML")
  - `rubin.selection_metric` (Tag): Konfigurierte Metrik (z.B. "qini")
  - `rubin.learner` (Tag): Verwendeter Learner ("catboost", "lgbm", "both")
  - `rubin.validation_mode` (Tag): Validierungsmodus ("cross", "external", "TMES")

### UX-Verbesserungen
- **Benchmark ohne Preset:** Das Add-On-Preset `benchmark` war ein Phantom (`cfg: {}`, keine Wirkung) und wurde entfernt. Der Benchmark-Vergleich gegen den historischen Score läuft **automatisch**, sobald eine S-Datei in `data_files.s_file` angegeben ist. Die Benchmark-Sektion auf der Datenpfade-Seite zeigt jetzt einen grünen Success-Badge bei gesetzter S-Datei, sonst einen gelben Hinweis mit Verweis.
- **External Eval immer zugänglich:** Die Eval-Datei-Felder sind jetzt permanent auf der Datenpfade-Seite sichtbar, nicht mehr conditional versteckt hinter `validateOn==="external"`. Ein Toggle „External Eval aktivieren" direkt neben der Überschrift macht den Modus explizit. Bei Eintragen/Hochladen einer Eval-X/T/Y-Datei wird `validateOn` automatisch auf "external" gesetzt.
- **Validation-Addon-Kategorie entfernt:** Nach den obigen Änderungen war die Validation-Gruppe bei den Add-Ons doppelt redundant (Toggle auf Datenpfade + Button auf Validierungs-Seite + Auto-Aktivierung). Das einzige verbliebene Preset `external_eval` und die leere Gruppe wurden entfernt. Layout: R4 (Erklärbarkeit) und R5 (Production) stehen jetzt in einer Reihe nebeneinander.
- **Ensemble-Preset entfernt:** Das Add-On-Preset `ensemble` (setzte nur `ensembleEnabled: true`) war redundant, da der Ensemble-Toggle bereits direkt auf der Modell-Seite existiert. Production & Export enthält jetzt nur noch "Bundle & Surrogate".
- **Kollisions-Warnung:** Bei gleichzeitig gesetztem `eval_mask_file` und `validateOn="external"` zeigt die UI jetzt eine Warn-Box auf der Validierungs-Seite. Backend ignoriert die Maske in diesem Fall (External-Eval-Dateien haben Vorrang), die UI macht das explizit.

### UI-Pre-Validation erweitert
Neue Checks in der Pre-Run-Validierung, die Backend-Validators spiegeln. Damit sieht der User die Fehler bereits im Validierungs-Panel statt erst beim Pydantic-Abbruch beim Run-Start:
- **Manual Champion muss in Models-Liste sein**: Wenn `selection.manual_champion` gesetzt ist, prüft die UI jetzt, dass der Name auch in `models.models_to_train` enthalten ist.
- **`fixed_params` bei `baseLearner="both"` muss nested sein**: Flache Parameter wie `{num_leaves: 31}` würden silent an beide Learner durchgereicht und CatBoost-Trials zum Absturz bringen. Die UI prüft jetzt auf `{lgbm:{...}, catboost:{...}}`-Struktur.
- **`reference_group` bei binary nur 0 erlaubt**: Bei `treatment.type="binary"` sind andere Werte als 0 aktuell nicht unterstützt.
- **MT-incompatible Selection-Metrics**: Bei `treatment.type="multi"` sind die BT-only-Metriken (`qini`, `auuc`, `uplift_at_10pct`, `uplift_at_20pct`, `uplift_at_50pct`, `policy_value`) nicht verfügbar. Die UI empfiehlt stattdessen `policy_value_T1`, `qini_T1` etc.

### Tuning-Fairness & Logging
- **Timeout bei `both`-Modus deaktiviert:** Bei `base_learner.type="both"` wird `timeout_seconds` (BLT + FMT) automatisch ignoriert und eine Warnung geloggt. Grund: LGBM-DART ist ~3× langsamer als CatBoost — ein Timeout würde zu weniger fertig durchgelaufenen LGBM-Trials führen und damit den Vergleich verzerren. Steuerung erfolgt ausschließlich über `n_trials`. UI zeigt bei Timeout + "both" einen gelben Hinweis.
- **FMT-Parallelisierung reduziert:** `FinalModelTuner._tuning_n_jobs` nutzt jetzt `n_cpus // 8` statt `n_cpus // 4`. Jeder FMT-Trial fittet ein vollständiges Kausalmodell (model_y + model_t + model_final mit Cross-Fitting) und verbraucht ~5-10× mehr RAM als ein BLT-Trial. Bei 40 Kernen: 5 statt 10 parallele Trials, dafür je 8 statt 4 Kerne pro Fit.
- **RCT-Propensity-Cap in Tuning-Plan-UI:** `TuningPlanPreview` berücksichtigt jetzt den Backend-Cap (`min(n_trials, 20)` bei RCT + Propensity). Fits-Berechnung, Zeilen-Badge und Info-Box sind korrekt. Zuvor wurde die volle Trial-Zahl angezeigt.
- **BLT-Log bereinigt:** Der irreführende `Ø Score` (Mittelwert über PR-AUC und neg_MSE — verschiedene Skalen) und die pauschale `Trials/Task`-Angabe (bei RCT variieren die Trials pro Task) wurden entfernt. Stattdessen werden die Einzel-Scores pro Task ausgegeben.

### Tiefenprüfungen ohne Findings
- UI cfg-Keys vs YAML_TO_CFG-Mapping: alle Keys gemappt oder als UI-only dokumentiert
- Backend-Felder ohne UI-Exposition: 9 technische Defaults (`storage_path`, `study_name_prefix` etc.) bewusst Backend-only
- Pydantic strict-mode Validierung der generierten YAML: alle Felder in Schemata definiert, `extra="forbid"` hält
- `fixed_params` bei `base_learner.type="both"`: Struktur `{catboost:{...}, lgbm:{...}}` ist im Backend korrekt handled (`build_base_learner` wählt anhand `_learner_type` und verwirft den nicht-gewählten Sub-Dict)
- Feature-Selection bei TMES: läuft auf bereits reduziertem X (nach Train/Holdout-Split) — leakage-frei
- `dml_crossfit_folds`: wird zur Laufzeit auf `cross_validation_splits` gesetzt; bewusst kein separater UI-Toggle

---

## 2026-04-16 (Session 8)

### Korrektheit
- **TMES Data-Leakage-Fix:** Bei `eval_mask_file` (Train-Many-Evaluate-Some) wurden zuvor Mask-Rows sowohl zum Training als auch zur Evaluation genutzt → optimistische Metriken. Neue Logik spaltet vor dem Training: `~mask` → X/T/Y für Training, `mask` → Holdout-Eval-Set. TMES läuft jetzt effektiv wie External Eval. MLflow loggt `tmeo_train_n` und `tmeo_eval_n`. Betroffen: `rubin/pipelines/analysis_pipeline.py`.
- **Bundle-Export Konsistenz bei TMES:** Der `X_full`-Snapshot für Bundle-Refit wird jetzt NACH dem TMES-Split erstellt, damit das Bundle-Modell nur auf Trainingsdaten refittet wird (nicht auf dem gesamten Datensatz inkl. Eval-Zeilen).
- **HTML-Report TMES-Erkennung:** Der Report zeigt jetzt alle drei Validierungsmodi korrekt an (`cross (5 Folds)`, `external (separater Datensatz)`, `TMES (Mask-Subset)`). Summary-Bar zeigt bei TMES zusätzlich `Eval (TMES): N Beob.`.

### YAML-Roundtrip-Bugs (UI ↔ Backend)
- **Search-Space bei `baseLearner="both"`:** Der UI-YAML-Emitter emittierte bei "both" gar keinen `search_space`-Block → customized Ranges gingen silent verloren. Fix: Emittiert jetzt beide Learner-Sektionen wenn customized.
- **Search-Space Import:** `parseYamlToCfg` las `search_space` gar nicht (nur 1-Ebenen-Parser). Neuer verschachtelter Parser für `tuning.search_space.{catboost,lgbm}` und `final_model_tuning.search_space.{catboost,lgbm}`.
- **Regex-Parser-Fix:** `[a-z_]+` erlaubte keine Ziffern → Parameter mit Zahlen im Namen (`l2_leaf_reg`, `reg_alpha`) wurden silent verschluckt. Fix: `[a-z0-9_]+`.
- **`log`-Flag Durchgängigkeit:** UI-Search-Space-Defs hatten kein `log: true` Flag → bei Änderung von log-skalierten Params (`learning_rate`, `l2_leaf_reg`, `random_strength`, ...) emittierte die UI die Range ohne `log: true` → Backend wechselte still von log-uniform zu linearem Sampling. Fix: UI-Defs ergänzt, Emitter gibt `log: true` aus, `low` wird automatisch auf `1e-6` geclampt falls User `low=0` setzt (Optuna-Constraint bei `log=True`). SSEditor zeigt `(log)`-Suffix am Parametername.
- **PData-Scope:** `setSp`/`setSpFmt` wurden nicht an `PData` durchgereicht → YAML-Import konnte Search-Spaces nicht in UI-State restaurieren. Fix: Props durchgereicht, Import extrahiert `__sp`/`__spFmt` und ruft Setter.

### Tuning-Qualität
- **TPESampler `group=True`:** Bei `base_learner.type="both"` fiel TPE bei CatBoost-spezifischen Parametern (`l2_leaf_reg`, `random_strength`, `rsm`, `min_data_in_leaf`, `model_size_reg`, `leaf_estimation_iterations`) auf `RandomSampler` zurück, weil der Parameter-Set zwischen Trials variierte (conditional search space). Fix: `group=True` partitioniert die Trial-Historie — TPE lernt separate KDE-Modelle für LGBM- und CatBoost-Zweig. Warnungen verschwinden, Tuning-Qualität bei "both" deutlich verbessert. Doppelter Fallback bei alten Optuna-Versionen.

### Explainability
- **SHAP-Analyse:** Zweistufiger Fallback — EconML SHAP-Plot-Satz (Summary, CATE-Profile, Dependence, Scatter) → generische SHAP-Werte (TreeExplainer). Importance-CSV und Plots als MLflow-Artefakte. Auch als CLI-Runner (`run_explain.py`).

### UX / UI
- **FLAML+both Warnung:** UI zeigt jetzt vor dem Run einen roten Hinweis bei Kombination `baseLearner="both"` + `tuningAutoml="flaml"`, dass Optuna als Fallback verwendet wird (FLAML unterstützt keine kategorische Learner-Wahl).
- **Modus-Badges in Pre-Run-Summary und während des Runs:** Cross (🔵) / External (🟣) / TMES (🟠) farblich differenziert.
- **Progress-Steps-Label:** "Training & Cross-Predictions" → "Training & Predictions" (neutral für alle Validierungsmodi).
- **Addon-Preset "Benchmark" entfernt:** Hatte leeres `cfg: {}`, bewirkte nichts — Benchmark läuft ohnehin automatisch bei gesetztem S-File.

### Dokumentation
- `docs/evaluation.md`: TMES-Sektion vollständig umgeschrieben (Leakage-free Routing)
- `docs/konfiguration.md`: `eval_mask_file`, `validate_on`, `shap_values.method` aktualisiert
- `docs/tuning_optuna.md`: TPE `group=True` dokumentiert, FLAML+both-Einschränkung ergänzt
- `docs/explainability.md`: `shap_values.method` ergänzt
- `docs/architektur.md`: HTML-Report TMES-Badges dokumentiert
- `docs/app_build.md`: Sync-Workflow für Index.html und Overview.html ergänzt

---

## Ältere Änderungen

Frühere Änderungen sind nicht in einem formalen Changelog erfasst. Siehe Git-Log oder die thematisch gegliederten Docs für Details.
